---
title: 站点与品牌配置
description: Root 配置站点身份、公开内容、法律文档、系统公告、顶部导航和侧边栏模块。
---

import { Callout } from 'fumadocs-ui/components/callout';

- 站点与品牌配置位于 `/system-settings/site`，仅 Root 可访问。
- 系统信息、公告、关于页、首页内容、用户协议和隐私政策等属于面向未登录访问者的公开内容，其接口无需登录即可读取。

## 站点与品牌配置简介

站点与品牌域下有四个独立分区：

| 分区 | 路径 | 职责 |
| --- | --- | --- |
| [系统信息](#系统信息) | `/system-settings/site`（默认进入） | 站点身份、公开内容与法律文档。 |
| [系统公告](#系统公告) | `/system-settings/site/notice` | 全局公告内容。 |
| [顶部导航](#顶部导航) | `/system-settings/site/header-navigation` | 顶部导航内置入口的显示与登录要求。 |
| [侧边栏模块](#侧边栏模块) | `/system-settings/site/sidebar-modules` | 侧边栏各区域与模块的显示控制。 |

<Callout type="warn">
  这些信息面向未登录访问者。不要写入 API Key、客户端密钥、内网地址、内部管理员资料或任何不应公开的信息。
</Callout>

## 配置分区

### 系统信息

「系统信息」分区集中配置站点身份与公开内容。字段如下：

| 字段 | 必填/校验 | 说明与边界 |
| --- | --- | --- |
| 系统名称 | 必填 | 在整个应用程序中显示的名称。显示在站点标题、浏览器标签、认证页面和品牌组件中。 |
| 服务器地址 | 可选 | 服务器的公开URL，用于OAuth回调、Webhook和其他外部集成。 |
| 异步任务对外地址 | 可选 | 异步任务媒体的公开基础地址。支持独立媒体域名、端口或 Nginx 路径前缀；留空时回退到服务器地址。 |
| 徽标 URL | 可选 | 您的徽标图片 URL |
| 页脚 | 可选 | 自定义页脚 HTML，显示在页面底部的页脚文本 |
| 关于页 | 可选 | 可填写 HTTP(S) URL、HTML 或 Markdown，决定「关于」页面的内容。 |
| 首页内容 | 可选 | 可填写 HTTP(S) URL、HTML 或 Markdown。决定主页上显示的内容。 |
| 用户协议 | 可选 | 支持完整 URL、HTML 或 Markdown；留空会关闭用户协议入口和同意要求。 |
| 隐私政策 | 可选 | 支持完整 URL、HTML 或 Markdown；留空会关闭隐私政策入口和同意要求。 |

- 站点与品牌-系统信息

![站点与品牌-系统信息页面](/assets/guide/feature-guide/system-settings-site-brand-system-information.png)

- 异步任务对外地址

「异步任务对外地址」用于构建异步任务产物的公开内容访问地址。当任务结果需要以公开 URL 被访问（例如视频/图像等内容下载）时，前端会以该地址为基址生成 `/v1/tasks/{id}/artifacts/...?access=...` 形式的长期有效能力 URL；未配置时回退使用「服务器地址」。

校验规则（前端与后端均会执行）：

- 可以为空；
- 必须是绝对 `http://` 或 `https://` 地址；
- 不含查询参数（`?`）、片段（`#`）或用户信息（`@`）；
- 不含控制字符、反斜杠或首尾空白；
- 提交时前端会自动移除结尾的 `/`。

<Callout type="warn">
  该地址必须能被最终用户从浏览器公开访问，填错或密钥不一致会导致任务产物 URL 无法访问或校验失败；这里只校验格式，不会探测可达性。
</Callout>

- 法律文档

用户协议或隐私政策字段非空后，登录、注册和页脚会提供相应入口；认证页面还会要求用户勾选同意已配置的文档。清空某一字段会停用对应入口和要求。

<Callout type="info">
该功能用于发布当前内容或外部链接，不是法律版本、签署记录或历史留存系统。如果需要可审计的协议版本和再次同意机制，应在 New API 之外建立相应流程。
</Callout>

### 系统公告

「系统公告」分区保存系统公告内容，没有独立的启用开关。内容非空时，前端通过公开的 `/api/notice` 接口读取并显示在通知中心；清空内容即可停止发布。

- 站点与品牌-系统公告

![站点与品牌-系统公告页面](/assets/guide/feature-guide/system-settings-site-brand-system-announcement.png)

<Callout type="info">
  本分区的系统公告与「控制台内容 」→ 「结构化公告」是两套配置：前者是一段全局通知内容，后者管理可启停的结构化公告列表，不应互相替代。
</Callout>

### 顶部导航

顶部导航控制内置入口的显示，支持以下开关：

| 入口 | 可配置行为 |
| --- | --- |
| 首页、控制台、文档、关于 | 显示或隐藏。 |
| 模型广场、排行榜 | 各自有「启用」和「需登录」两个开关；开启需登录后，访客必须登录后才能访问，未登录用户点击后会跳转至登录。 |

- 站点与品牌-顶部导航

![站点与品牌-顶部导航页面](/assets/guide/feature-guide/system-settings-site-brand-top-navigation.png)

### 侧边栏模块

侧边栏按区域和模块控制内置入口是否显示。当前分区结构与默认模块如下：

| 区域 | 当前内置模块 |
| --- | --- |
| 聊天 | Playground、聊天。 |
| 控制台 | 数据看板、API 令牌、使用日志、审计日志、绘图日志、任务日志。 |
| 个人 | 钱包、个人设置、安全。 |
| 管理员 | 渠道、模型、兑换码、用户、系统设置、订阅。 |

- 站点与品牌-侧边栏模块

![站点与品牌-侧边栏模块页面](/assets/guide/feature-guide/system-settings-site-brand-sidebar-module.png)

<Callout type="info">
- 关闭整个区域会同时禁用该区域下的所有子模块；重新打开后，各模块仍按自身开关决定是否显示。
- 页面提供「恢复默认」，该按钮先把表单恢复为代码中的默认组合（全部启用），然后保存才能写入服务端。
- 全局侧边栏配置决定模块是否可展示；拥有个人侧边栏设置权限的用户还可以在全局允许范围内进一步隐藏自己的「聊天/控制台/个人」区域模块（管理员区域的模块由全局配置决定，个人设置不覆盖）。
- **Root 不应用个人侧边栏覆盖**：个人侧边栏配置不会隐藏其任何模块。
</Callout>

## 保存与生效

1. 修改前记录当前值，特别是服务器地址、异步任务对外地址、法律内容和导航开关。
2. 保存后刷新当前页面，确认非敏感字段已写入；逐字段保存时务必确认每个已修改字段都提示成功。
3. 涉及外部地址（OAuth 回调、任务产物 URL、iframe 嵌入、支付回跳）时，先在测试实例或维护窗口验证，不要只在表单内检查。

<Callout type="info">
  页面显示「保存成功」只表示相应配置项已写入，不代表 OAuth、SMTP、支付网关、Webhook 或其他外部服务已经连通。必须通过实际登录、发信、支付沙箱、回调或业务请求进行验证。
</Callout>

## 注意事项

- **页脚是裸 HTML**：不要加入不可信脚本、事件属性或第三方注入代码。
- **关于页/首页/法律正文的嵌入 HTML**：DOMPurify 会移除 `<script>` 等标签，脚本不会执行；样式与 iframe 行为也受到隔离与沙箱限制，不要把逻辑依赖在任意脚本上。
- **外部 URL 嵌入**：远端站点可能因 `X-Frame-Options`、CSP、混合内容或证书拒绝显示；这是远端策略，New API 无法绕过。
- **首页内容有浏览器缓存**：修改后需刷新或清 `home_page_content` 缓存；清空字段才会恢复内置首页。
- **公告已读与内容绑定**：任何标题或正文变化都可能被视为新公告而再次显示未读。
- **菜单隐藏不等于关闭能力**：隐藏入口后仍可通过地址访问，是否允许进入取决于对应路由与后端接口的权限处理。
- **异步任务对外地址**：仅用于任务产物 URL，需公网可达且各节点共享 `CRYPTO_SECRET`。
- **变更前记录**：系统设置当前没有统一配置快照、历史版本比较或一键回滚功能。生产环境修改前应自行记录原值，按原配置逐项恢复。

## 相关文档

- [系统设置概览](./system-setting)：系统设置的生效机制、多实例同步、敏感配置与变更验证要求。
- [身份验证配置](./settings-auth)：OAuth 回调地址、Passkey RP ID 与 origin 对服务器地址的依赖。
- [计费与支付配置](./settings-billing)：文档链接、额度单位、模型与分组价格配置。
- [控制台内容配置](./settings-content)：结构化公告、数据看板、API 信息与 FAQ。
- [用户管理](/zh/docs/guide/feature-guide/admin/user)：用户角色、权限矩阵与侧边栏相关权限。
