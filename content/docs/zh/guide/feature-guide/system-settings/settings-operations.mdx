---
title: 运维配置
description: Root 配置系统模式、额度告警、SMTP、Worker、日志维护、性能保护和版本检查。
---

import { Callout } from 'fumadocs-ui/components/callout';

运维配置位于 `/system-settings/operations`，仅 Root 可访问。

## 运维配置简介

该工作区包含「系统行为」「监控与警报」「SMTP 邮箱」「Worker 代理」「日志维护」「性能」和「系统维护」七个分区。用于配置系统模式、额度告警、SMTP、Worker、日志维护、性能保护和版本检查。

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 分区 | 入口 | 主要用途 |
| --- | --- | --- |
| [系统行为]| `/system-settings/operations/behavior` | 侧边栏默认偏好、演示站与自用模式。 |
| [监控与警报] | `/system-settings/operations/alerts` | 额度提醒与模型性能指标。 |
| [SMTP 邮箱]| `/system-settings/operations/email` | 系统邮件的发送与认证。 |
| [Worker 代理] | `/system-settings/operations/worker` | 指定下载与通知链路的外部代理。 |
| [日志维护] | `/system-settings/operations/logs` | 消费日志开关、数据库日志与服务器文件清理。 |
| [性能] | `/system-settings/operations/performance` | 请求体缓存、资源保护和进程统计。 |
| [系统维护] | `/system-settings/operations/update-checker` | 版本信息与更新检查。 |

</div>

<Callout type="warn">
  日志删除、缓存清理和强制 GC 都是即时运维操作，不属于可撤销的表单草稿。执行前先确认目标、影响范围和恢复方案，并避免在高峰期反复操作。
</Callout>

## 配置分区

运维域下有七个独立分区：

### 系统行为

系统行为分区包含三个开关：

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 配置键 | 默认值 | 实际作用与边界 |
| --- | --- | --- | --- |
| 默认折叠侧边栏 | `DefaultCollapseSidebar` | `false` | 默认情况下为新用户折叠侧边栏。 |
| 演示站模式 | `DemoSiteEnabled` | `false` | 启用功能受限的演示模式。 |
| 自用模式 | `SelfUseModeEnabled` | `false` | 优化系统以适应自托管单用户使用。 |

</div>

- 运维-系统行为配置

![系统行为配置](/assets/guide/feature-guide/system-settings-operations-system-behavior.png)

### 监控与警报

该分区包含用户剩余额度提醒和模型性能指标配置。

- 额度提醒

「额度提醒阈值」使用内部 quota／Token 单位，配置键为 `QuotaRemindThreshold`。Relay 结算链路会分别检查钱包和订阅额度：钱包按 `UserQuota - (quota + preConsumedQuota)` 比较，订阅按订阅总额减已使用量比较。


- 模型性能指标

模型性能指标从 Relay 请求完成和计费链路收集模型、分组、请求耗时、首 Token 时间、成功率、输出 Token 和生成耗时，用于模型广场等性能展示。

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 配置键 | 默认值 | 行为与边界 |
| --- | --- | --- | --- |
| 启用模型性能指标 | `perf_metrics_setting.enabled` | `true` | 控制是否继续记录 Relay 性能样本。关闭后不会新增样本，刷盘循环也会跳过写库和过期清理；已有数据库记录与关闭前的内存聚合桶不会立即清空。 |
| 刷盘间隔（分钟） | `perf_metrics_setting.flush_interval` | `5` | 最小值为 `1`，控制把已完成的内存聚合桶写入数据库以及执行过期数据清理的循环周期，不是前端图表刷新频率。 |
| 聚合时间桶 | `perf_metrics_setting.bucket_time` | `hour` | 可选 1 分钟、5 分钟和 1 小时；修改后只影响后续样本的分桶。 |
| 保留天数 | `perf_metrics_setting.retention_days` | `0` | `0` 表示永久保留；正数会在刷盘循环中清理更早的性能指标。 |

</div>

操作流程：
1. 根据额度单位设置提醒阈值，并确认测试用户的通知方式和个人阈值。
2. 按数据量选择是否记录模型性能指标、刷盘间隔、聚合时间桶与保留天数，然后保存。
3. 用测试账号验证通知；性能指标应在聚合桶完成并刷盘后再检查，不能用保存后的即时空图判断失败。

- 运维-监控与警报配置

![监控与警报配置](/assets/guide/feature-guide/system-settings-operations-monitoring-alerts.png)

<Callout type="info">
- 低于阈值时异步发送额度不足通知。用户个人设置中非零的提醒阈值会优先于此全局值。
- 通知方式由用户设置决定，可以是邮件、Webhook、Bark 或 Gotify；未选择时默认使用邮件。通知失败会写入系统错误日志。
- 选择较细的桶或较短的刷盘间隔会增加内存、数据库和后台写入负载，应结合模型数量、分组数量和请求量评估。
</Callout>

### SMTP 邮箱

SMTP 用于注册验证码、密码重置、用户通知和其他系统邮件。主要配置键如下：

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 配置键 | 默认值或格式 | 说明 |
| --- | --- | --- | --- |
| SMTP 主机 | `SMTPServer` | 空 | SMTP 提供商的主机名或 IP。 |
| 端口 | `SMTPPort` | `587` | 常用端口包括 25、465 和 587。 |
| 跳过 SMTP TLS 证书验证 | `SMTPInsecureSkipVerify` | `false` | 允许自签名或主机名不匹配的 SMTP 证书。 |
| 用户名 | `SMTPAccount` | 空 | 用于与 SMTP 服务器进行身份验证的账户。 |
| 密码 / 访问令牌 | `SMTPToken` | 不回显 | 留空以保留现有凭证。 |
| SMTP 加密方式 | `SMTPSSLEnabled`、`SMTPStartTLSEnabled` | 均关闭 | 选择一种 SMTP 传输加密方式；可选'无加密'、'SSL/TLS'、'STARTTLS'。 |
| 强制 AUTH LOGIN | `SMTPForceAuthLogin` | `false` | 强制使用 AUTH LOGIN 方法进行 SMTP 认证。 |
| 发件地址 | `SMTPFrom` | 空 | 用于发送消息的显示名称和电子邮件。 |

</div>

- 运维-SMTP 邮箱配置

![SMTP 邮箱配置](/assets/guide/feature-guide/system-settings-operations-smtp-email.png)

- 传输加密模式

| 模式 | 行为与建议 |
| --- | --- |
| 无加密 | 通常建立明文 SMTP 连接；端口 `465` 且未启用 STARTTLS 时仍按下述兼容规则使用隐式 TLS。明文连接只适合受控内网或明确要求该模式的服务。 |
| SSL/TLS | 建立隐式 TLS 连接。即使没有显式选择，端口为 `465` 且未启用 STARTTLS 时，当前后端也会使用隐式 TLS。 |
| STARTTLS | 先建立普通 SMTP 连接，再要求服务器支持并升级到 STARTTLS；服务器未声明 STARTTLS 时发信失败。 |

<Callout type="info">
SMTP 连接由后端直接建立，不经过 Worker 代理或 URL SSRF 校验。生产环境应确认 DNS、端口、证书、认证、SPF、DKIM、DMARC 和发件人策略。
</Callout>

- 配置与验证

1. 从邮件服务商获取 SMTP 主机、端口、认证账号和授权码或密码；不要把 Web 邮箱登录地址填入主机字段。
2. 按服务商要求选择 SSL/TLS 或 STARTTLS，填写纯邮箱发件地址。除已评估的内部场景外，不启用跳过证书验证。
3. 填写新 Token 并保存；不轮换凭证时保留输入框为空。切换安全模式后刷新页面，确认相关开关均已保存。
4. 使用测试账号请求验证码、密码重置邮件或额度通知，检查投递结果与服务端错误日志；保存成功本身不代表连接成功。

### Worker 代理

Worker 代理用于源码中明确接入的服务端外部请求链路，例如远程文件或图片下载、用户 Webhook，以及 Bark 和 Gotify 通知。它不是渠道 Relay、支付、OAuth 或所有出站流量的通用代理。

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 配置键 | 默认值或校验 | 行为 |
| --- | --- | --- | --- |
| Worker URL | `WorkerUrl` | 空；非空时必须以 `http://` 或 `https://` 开头 | 保存时移除末尾 `/`；非空时请求将被转发到此 Worker。 |
| Worker 访问密钥 | `WorkerValidKey` | 不回显 | 随 Worker 请求发送，用于与 Worker 进行身份验证。留空以保留现有密钥。 |
| 允许 HTTP 图片请求 | `WorkerAllowHttpImageRequestEnabled` | `false` | 当代理通过 HTTP 获取图像的 worker 时启用。 |

</div>

- 运维-Worker 代理配置

![Worker 代理配置](/assets/guide/feature-guide/system-settings-operations-worker-proxy.png)

- 配置与停用

1. 准备受控的 Worker 服务，确认其接受 New API 的请求格式并校验访问密钥，再填写 URL 与密钥。
2. 按目标资源的协议决定是否允许 HTTP，点击「保存 Worker 设置」，分别验证实际使用的下载或通知链路。
3. 停用时清空 Worker URL；需要同时删除保存的访问密钥时，将密钥输入框也留空后保存。仅清空密钥输入框但保留 URL，不会清除旧密钥。


<Callout type="info">
- Worker 负载可能携带目标 URL、请求头、通知内容和认证信息。
- `WorkerValidKey` 作为请求 JSON 的 `key` 字段发送，是否校验由 Worker 实现决定；New API 不使用它验证 Worker 响应，也不能用它替代目标 URL 的访问控制。
- 普通下载、Webhook 和通知链路仍会在进入 Worker 前使用相应的 SSRF 校验，但 Worker URL 本身是 Root 配置的外部信任边界。
</Callout>

<Callout type="warn">
  只使用由你控制并能审计日志的 Worker。启用 Worker 后，敏感通知或远程资源请求可能经过该服务；确认其 TLS、密钥验证、日志脱敏、超时、目标过滤和数据保留策略。
</Callout>

### 日志维护

日志维护分为记录配额使用量、清理历史日志和服务器日志管理。三者影响不同的数据源。

- 记录配额使用量

1. 「记录配额使用量」对应 `LogConsumeEnabled`，默认开启。
2. 它控制是否创建逐请求的消费日志，包括普通 Relay 和异步任务结算产生的消费记录。
3. 关闭后会减少数据库写入，但使用日志、消费统计和依赖这些记录的分析将缺少新数据。
4. 调整开关后点击「保存日志设置」。

<Callout type="info">
该开关不会停止错误、充值、管理审计、系统、退款或登录日志，也不会关闭服务器文本日志、Midjourney 任务表或异步任务表。
</Callout>

- 清理历史日志

清理作用于执行节点连接的 `LOG_DB`，移除所选时间戳之前创建的所有日志条目。

1. 先归档必要数据，在日期与时间控件中选择截止时间，或使用「24 小时前」「7 天前」「30 天前」快捷项。
2. 点击「清理日志」，核对确认框中的截止时间，再确认删除；系统会创建 log_cleanup 系统任务。
3. 观察进度与已处理数量。任务处于待执行或运行中时，页面不会再发起同类任务；关闭页面不等于取消后台清理。
4. 成功时核对删除数量或“没有匹配日志”的提示；失败时查看任务错误。

| 任务状态 | 含义与处理 |
| --- | --- |
| `pending` | 等待运行器领取；长期等待时检查主节点和系统任务运行器。 |
| `running` | 正在清理并更新进度，不要重复发起。 |
| `succeeded` | 本次任务完成，核对最终删除数量。 |
| `failed` | 查看错误并检查日志库与运行器；已完成的删除不会自动恢复。 |

<Callout type="info">
- 此操作不会清理 `quota_data` 聚合数据、性能指标、绘图与异步任务记录，也不会删除服务器上的日志文件。
- 多个实例共享日志库时，删除会影响共同的日志历史，不能把它当作“只清理当前实例”。
</Callout>

- 服务器日志管理

服务器日志管理只在配置了日志目录时可用，并且只扫描该目录下名称匹配 `oneapi-*.log` 的文件。<br/>
可查看服务器日志目录、日志文件数、日志总大小和时间范围；服务器日志提供两种清理方式：

1. 按数量：保留最近 N 个文件，删除修改时间早于最近第 N 个文件之前的日志文件。
2. 按天数：保留最近 N 天，删除修改时间早于 N 天前的日志文件。

![日志维护配置](/assets/guide/feature-guide/system-settings-operations-log-maintenance.png)

<Callout type="warn">
  数据库日志和服务器日志文件的删除不可恢复。清理前确认审计、争议处理、支付、合规和故障排查所需的留存周期，并完成必要的数据库或日志归档。
</Callout>

### 性能

性能分区同时管理请求体磁盘缓存、请求体内存缓存和系统性能监控。

- 请求体磁盘缓存

启用后，达到阈值且未超过当前缓存容量边界的大请求体或文件数据会临时存到磁盘，以减少内存占用；不满足条件时继续使用内存。

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 配置键 | 默认值或校验 | 行为与边界 |
| --- | --- | --- | --- |
| 启用磁盘缓存 | `performance_setting.disk_cache_enabled` | `false` | 启用磁盘缓存后，大请求体将临时存储到磁盘而非内存，可显著降低内存占用。建议在 SSD 环境下使用。 |
| 磁盘缓存阈值 (MB) | `performance_setting.disk_cache_threshold_mb` | `10 MB`；最小 `1 MB` | 请求体超过此大小时使用磁盘缓存 |
| 磁盘缓存最大总量 (MB) | `performance_setting.disk_cache_max_size_mb` | `1024 MB`；最小 `100 MB` | 按当前进程统计判断“现有用量 + 新请求大小”是否超过该值；它不是操作系统磁盘配额。 |
| 缓存目录 | `performance_setting.disk_cache_path` | 空 | 留空使用系统临时目录下的 `new-api-body-cache`；非容器环境可编辑，容器环境不显示此字段，应通过挂载和容器存储策略管理。 |

</div>

- 系统性能监控

系统性能监控默认开启。后台约每 5 秒采集 CPU、虚拟内存和缓存目录所在磁盘的使用率。当某个阈值大于 `0` 且最近快照的整数使用率严格高于阈值时，新 Relay 请求会被拒绝并返回 HTTP `503`。

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 配置键 | 校验 |
| --- | --- | --- |
| 启用性能监控 | `performance_setting.monitor_enabled` | `true`or`flase` |
| CPU 阈值 | `performance_setting.monitor_cpu_threshold` | `90`；前端最小值 `0`，没有前端最大值限制 |
| 内存阈值 | `performance_setting.monitor_memory_threshold` | `90`；`0–100` |
| 磁盘阈值 | `performance_setting.monitor_disk_threshold` | `95`；`0–100` |

</div>

<Callout type="info">
- 资源保护使用周期性快照，不是排队或自动扩容系统。启用后通常每 5 秒更新一次；
- 资源保护中间件挂载在 Playground、`/v1`、Gemini、Midjourney 和 Suno 等 Relay 路径，不影响登录、普通管理 API 或全部后台任务。
</Callout>

- 性能统计与操作

页面显示当前 New API 进程中的磁盘／内存缓存用量和命中次数、活动缓存数量、缓存目录文件、磁盘空间、Go 内存以及 Goroutine 数量，并提供以下操作：

| 操作 | 实际影响 |
| --- | --- |
| 刷新统计 | 重新获取当前进程的统计信息。 |
| 清理不活跃缓存 | 删除缓存目录中修改时间超过 10 分钟的普通文件。实现只按文件修改时间判断，没有额外核对文件是否仍被请求引用；高峰期或存在长时间请求时应谨慎执行。 |
| 重置统计 | 只清零磁盘和内存缓存命中次数，不清空活动缓存、当前用量或目录文件。 |
| 执行 GC | 在当前 Go 进程中立即调用垃圾回收，可能造成短暂延迟抖动。 |

<Callout type="info">
这些统计和操作只作用于收到管理请求的当前实例，不会自动遍历其他节点。
</Callout>

- 配置与操作顺序

1. 根据实例的磁盘空间和请求体大小设置缓存阈值与最大总量；不要把最大缓存值当作已经预留的磁盘空间。
2. 设置资源保护阈值并保存，刷新统计核对当前实例；按每个维度的单位和范围填写，不把 CPU 校验范围套用到内存或磁盘。
3. 先使用「刷新统计」观察用量，再按需要单独执行「清理不活跃缓存」「重置统计」或「执行 GC」，核对提示和刷新后的数据。
4. 多实例部署要分别检查目标节点。缓存清理不可恢复，重置命中次数也不能代替释放缓存或清理日志。

- 运维-性能配置

![性能配置](/assets/guide/feature-guide/system-settings-operations-performance.png)

### 系统维护

系统维护分区展示 `/api/status` 返回的当前版本和进程启动时间，并允许浏览器检查 GitHub Release。

点击「检查更新」时，浏览器直接请求：

```text
https://api.github.com/repos/Calcium-Ion/new-api/releases/latest
```

- 运维-系统维护配置

![系统维护配置](/assets/guide/feature-guide/system-settings-operations-system-maintenance.png)

<Callout type="warn">
  更新检查不会下载代码、验证升级兼容性、替换二进制、拉取镜像、迁移数据库、重建容器、重启服务或自动回滚。查看 Release 后，应按照实际部署方式制定备份、测试、升级和恢复流程。
</Callout>

## 变更后验证

1. 每保存一个多字段分区后刷新页面，逐项确认是否存在部分成功；在多实例环境中为配置同步预留时间。
2. 检查公开状态是否包含默认侧边栏偏好，并用当前新版前端确认它仍由 `sidebar_state` Cookie 控制；再分别使用普通用户和 Root 验证演示站、自用模式及实际角色权限。
3. 用测试用户把余额降到阈值以下，分别验证邮件、Webhook、Bark 或 Gotify；确认通知失败不会改变结算结果。
4. 产生可控的 Relay 请求，等待至少一个性能指标刷盘周期，再检查模型广场中的延迟、成功率和分组数据。
5. 通过验证码或测试通知验证 SMTP 的 DNS、端口、TLS、认证、发件人和投递；轮换凭证后确认旧凭证已失效。
6. 对每条使用 Worker 的下载或通知链路分别测试 HTTPS、目标拒绝、Worker 密钥错误和超时，确认请求没有意外绕过网络策略。
7. 在备份或测试数据上执行数据库与服务器日志清理，核对删除阈值、任务进度、保留文件和失败列表。
8. 使用超过磁盘缓存阈值的测试请求验证内存／磁盘选择，并模拟资源超过阈值时的 Relay `503`；同时确认管理页面仍可访问。
9. 从运行管理页面的浏览器检查 GitHub 网络和 Release 展示，再按部署文档手动完成升级演练。

## 常见问题

- **修改默认侧边栏后当前前端没有变化**：该值已发布到公开状态，但当前新版前端只读取 `sidebar_state` Cookie，尚未消费该设置；清除 Cookie 只会回到前端自身的默认展开状态。该配置主要供兼容客户端使用。
- **开启演示站后危险操作仍可执行**：演示模式不是完整的权限或只读策略。应使用角色权限、网络边界、只读数据副本和外部访问控制保护演示实例。
- **开启自用模式后仍能注册或看到多个用户**：该开关不会关闭多用户与注册接口；要限制注册请在[身份验证配置](./settings-auth)中关闭对应能力。
- **额度已低于阈值但没有通知**：检查用户是否设置了个人阈值和通知方式，再检查 SMTP、Webhook、Bark、Gotify、Worker、SSRF 与服务端错误日志。
- **模型性能指标没有立即出现**：样本先进入聚合桶，已完成的桶按刷盘周期写库。检查总开关、模型名、分组、桶时间和保留设置，并等待完整周期。
- **SMTP 保存后仍无法发信**：页面不测试连接。核对主机、端口、安全模式、证书、账号、Token、发件地址和服务商限制；端口 `465` 还会触发隐式 TLS 行为。
- **Worker 已配置但渠道请求仍直连上游**：Worker 只用于明确接入的下载和通知链路，不是模型渠道的通用网络代理。
- **清理数据库日志后看板历史数据仍存在**：日志任务只删除 `logs` 表，不会删除 `quota_data` 聚合、性能指标或任务记录。
- **日志清理一直等待或只完成了一部分**：检查主节点运行器、任务错误和日志库连接；任务失败不会回滚已删除的数据，重新提交前应再次确认截止时间。
- **清理服务器日志后仍保留一个旧文件**：当前活动日志始终跳过；按数量清理还会保留指定的最新文件数。
- **磁盘缓存达到上限后请求失败了吗**：系统通常改用内存；部分流式读取链路在磁盘创建失败且 Reader 已消费时可能直接报错。上限也不是磁盘配额保证。
- **资源使用率达到阈值但请求没有被拒绝**：比较条件是严格高于阈值，并使用最近一次采样的整数值；阈值为 `0` 时该维度不检查。
- **检查更新显示的版本并不比当前版本新**：页面只判断标签字符串是否相等，不做语义版本排序。请打开 Release 并人工核对版本、分支和升级路径。

## 相关文档

- [系统设置概览](./system-setting)：系统设置的生效机制、多实例同步、敏感配置与变更验证要求。
- [站点与品牌配置](./settings-site)：公开站点内容、导航和任务地址。
- [身份验证配置](./settings-auth)：注册、OAuth、Passkey 和邮箱验证。
- [计费与支付配置](./settings-billing)：额度、定价、支付合规和签到奖励。
- [模型与路由配置](./settings-models)：协议适配、重试、路由可靠性和模型部署。
- [安全与限制配置](./settings-security)：限流、敏感词、SSRF 和令牌数量限制。
- [控制台内容配置](./settings-content)：数据看板、公告、FAQ、API 地址和客户端内容。
- [系统信息](/zh/docs/guide/feature-guide/admin/system-info)：系统任务、节点状态和其他 Root 运维信息。
