---
title: 计费与支付配置
description: 配置额度、货币显示、模型与分组价格、支付网关和签到奖励，了解参数、保存流程与入账边界。
---

import { Callout } from 'fumadocs-ui/components/callout';

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

计费与支付配置位于 `/system-settings/billing`，仅 Root 可访问。

## 计费与支付配置简介

该工作区包含「额度设置」、「货币与展示」、「模型定价」、「分组定价」、「支付网关」、「签到奖励」六个分区。用于配置模型和工具定价、分组倍率、邀请奖励与额度预扣、货币汇率及显示、充值规则与支付网关，以及签到奖励。

| 分区 | 入口 | 主要用途 |
| --- | --- | --- |
| [额度设置](#额度设置) | `/system-settings/billing/quota` | 新用户额度、预扣、邀请奖励及外部链接。 |
| [货币与展示](#货币与展示) | `/system-settings/billing/currency` | 金额显示模式、汇率与 Token 统计。 |
| [模型定价](#模型定价) | `/system-settings/billing/model-pricing` | 模型价格、表达式、工具价格及上游同步。 |
| [分组定价](#分组定价) | `/system-settings/billing/group-pricing` | 调用与充值倍率、用户可用分组及 Auto 顺序。 |
| [支付网关](#支付网关) | `/system-settings/billing/payment` | 合规确认、充值规则及各网关凭证和回调。 |
| [签到奖励](#签到奖励) | `/system-settings/billing/checkin` | 每日签到开关和奖励范围。 |

配置时先区分四种量：
1. **内部额度 配额点数** 是余额和扣费的计量单位；
2. **显示金额**是 配额点数 格式化后的结果；
3. **模型费用**由请求用量和价格规则计算；
4. **网关实收**由各支付渠道的订单决定。

<Callout type="warn">
  计费与支付配置会影响扣费、余额和支付入账。修改前需记录原配置，用测试用户核对价格，并在支付服务商允许的测试环境中验证。注意保管 API 密钥、Webhook 密钥、RSA 私钥、完整签名或含凭证的请求 URL。
</Callout>

## 配置分区

### 额度设置

配置新用户初始额度、邀请双方奖励、请求预扣及免费模型预扣开关，以及充值和文档链接。

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 默认或填写要求 | 说明 |
| --- | --- | --- |
| 新用户额度 | 默认 `0`；填写非负整数 配额点数 | 授予新用户的初始配额，不追补已有用户。 |
| 预消耗配额 | 默认 `500`；填写非负整数 | 向用户收费前消耗的配额。 |
| 邀请者奖励 | 默认 `0`；填写非负整数 配额点数 | 符合条件的邀请注册后，授予邀请人的配额。 |
| 受邀者奖励 | 默认 `0`；填写非负整数 配额点数 | 授予被邀请人的配额。 |
| 免费模型预消耗 | 默认开启 | 启用后，零成本模型也会在最终结算前预先消耗配额。 |
| 充值链接 | 默认空 | 供用户购买配额的外部链接，不会自动接入该网站的支付回调。 |
| 文档链接 | 默认 `https://docs.newapi.pro` | 站点文档入口，也供顶部导航等位置使用。 |

</div>

**操作流程**

1. 核对额度单位。
2. 设置新用户额度、预扣和奖励；配置正数邀请奖励前，先到支付网关确认合规声明。
3. 填写充值、文档链接并保存，刷新检查非敏感字段。
4. 用新建测试用户验证初始额度；用邀请注册验证双方奖励，用请求日志核对预扣与最终费用。

- 计费与支付-额度设置
![额度设置](/assets/guide/feature-guide/system-settings-billing-and-payments-quota-settings.png)

<Callout type="info">
配置邀请奖励不为0时，需要先在「系统设置」→「计费与支付」→「支付网关」设置中确认合规条款。
</Callout>

### 货币与展示

配置额度的显示模式、货币符号与换算汇率，以及是否展示 Token 用量统计。

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 默认或填写要求 | 说明 |
| --- | --- | --- |
| 显示模式 | 默认 `USD` ；可选`USD`、`元`、`自定义货币`| 选择如何向用户展示配额值。 |
| 美元汇率 | 后端默认 `7.3`；表单最小 `0.0001` | 美元与您的支付网关货币之间的实际汇率。 |
| 自定义货币符号 | 默认 `¤`；仅自定义货币 时必填，最多 8 字符 | 显示价格时使用的前缀；仅 CUSTOM 模式显示。 |
| 每 USD 单位数 | 默认 `1`；仅自定义货币 时必填，最小 `0.0001` | 从美元到您的自定义货币的转换率，表示 `1 USD = X` 个自定义货币单位。 |
| 显示 Token 统计信息 | 默认开启 | 在用户界面中显示令牌使用统计信息。 |

</div>

**操作流程**

1. 选择货币显示模式 USD、元 或 自定义货币；使用 自定义货币 时需填写自定义货币符号以及每 USD 单位数。
2. 填写相关配置信息，核对 Token 统计信息开关，保存。
3. 在钱包、价格页和日志中对照同一笔额度，确认货币符号、小数和换算值。

- 计费与支付-货币与展示
![货币与展示](/assets/guide/feature-guide/system-settings-billing-and-payments-currency-and-display.png)

<Callout type="info">
内部 `500000 配额点数` 在默认基准下为 `$1`。
</Callout>

### 模型定价

模型定价配置页分为「模型价格」「未设置价格的模型」「工具价格」「上游价格同步」四个标签。配置模型计费模式、各类用量价格与计费表达式、工具调用价格，并支持补全未定价模型和同步上游价格。

<Callout type="warn">
配置或编辑模型价格时，计费模式推荐使用计费表达式；「按 Token」、「按次」为已弃用的计费模式，但仍可在对应界面将旧数据编辑转换为计费表达式。
</Callout>

#### 模型价格

列表用于搜索、检查和维护模型价格，不会自动给渠道增加模型。价格配置应与请求使用的模型标识和实际计费路径一致。

- 计费与支付-模型价格-模型价格
![模型价格](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-model-prices.png)

- 添加或修改流程

1. 搜索模型并编辑，或点击添加模型定价。新建时填写准确模型名，编辑时模型名不可直接更改。
2. 选择定价货币和模式，设置价格或表达式。模板只是起点，先核对当前供应商价格和缓存规则。
3. 输入已知用量检查估算；含请求条件时，再模拟请求体、请求头和时间。
4. 点击保存模型价格，并确认列表及刷新后的价格符合预期。
5. 用测试请求在日志中核对真实用量、分组倍率和最终费用。

- 计费与支付-模型定价-模型价格-添加模型价格
![添加模型定价](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-model-prices-add-model-pricing.png)

- 计费与支付-模型定价-模型价格-编辑模型价格
![编辑模型定价](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-model-prices-edit-model-pricing.png)

- 基础字段

<div className="[&_table]:min-w-[48rem] [&_td:first-child]:whitespace-nowrap [&_th]:whitespace-nowrap">

| 页面字段 | 默认或填写要求 | 说明 |
| --- | --- | --- |
| 模型名称 | 新建必填 | API 请求中使用的确切模型标识符。 |
| 定价货币 | USD | 系统底层统一使用美元计价。选择站点货币是为了方便输入和换算，金额会按站点汇率转换为美元。切换货币不会改变实际价格；原始计费表达式始终使用美元。 |
| 计费模式 | 计费表达式 or 按Token or 按次 | 推荐使用计费表达式；「按 Token」、「按次」为已弃用的计费模式，但仍可在对应界面将旧数据编辑转换为计费表达式。 |
| 编辑器模式 | 可视化编辑器或表达式编辑器 | 原始表达式和预设使用美元。货币选择仅换算可视化价格输入和金额预览。 |
| 预设模板 | 按实际模型选择 | 包括固定、阶梯、多模态、请求规则和时间条件；应用预设会替换当前编辑内容，仍需核对和保存。 |
| 档位名称 | 为各档位使用可识别名称 | 用于标识命中的价格档位，例如 `base`、`standard`。 |
| 档位计费模式 | 按 Token 或 按每次调用 | Token 单价按每百万对应 Token 填写；每次调用 按每次请求价格填写 USD/请求的固定金额。 |
| 阶梯条件与顺序 | 按业务设置 | 点击「添加计费分支」可添加计费阶梯条件，阶梯条件内可增加对时间、完整输入长度、计费输入token、计费输出token的条件组；从前向后匹配，首条命中决定档位；兜底档位处理此前均未命中的请求。 |

</div>

- Token价格与媒体价格参数说明

价格单位均为 单位货币 / 百万 Token（单位货币/1M token）。

| 参数名称 | 表达式变量 | 定义说明 |
| --- | --- | --- |
| 输入价格 | `p` | 输入 Token 的基础单价。 |
| 输出价格 | `c` | 模型输出 Token 的基础单价。 |
| 缓存读取价格 | `cr` | 复用已有提示词缓存时，命中缓存的输入 Token 单价。 |
| 缓存创建（5m） | `cc` | 创建有效期为 **5 分钟**的提示词缓存时，写入缓存的 Token 单价。 |
| 1 小时缓存写入价格 | `cc1h` | 创建有效期为 **1 小时**的提示词缓存时，写入缓存的 Token 单价。 |
| 图像输入价格 | `img` | 输入图片对应的图像 Token 单价。 ||
| 图片缓存输入价格 | `img_cr` | 命中缓存的图像输入 Token 单价；需要上游提供有效的图片缓存用量明细。 |
| 图像输出价格 | `img_o` | 模型生成图片对应的图像输出 Token 单价。 |
| 音频输入价格 | `ai` | 输入音频对应的音频 Token 单价。 |
| 音频输出价格 | `ao` | 模型生成音频对应的音频 Token 单价。 |

- 请求模拟参数说明

| 字段／控件 | 定义与作用 | 填写要求或说明 |
| --- | --- | --- |
| 请求模拟 | 展开或收起模拟区域，预览当前规则下的费用。 | 输入变化后自动重算，无需点击运行按钮。 |
| 模拟请求体 | 为计价规则提供请求参数。 | 必须是 JSON 对象，如 `{"priority":true}`；无参数时填写 `{}`。 |
| JSON 状态标识 | 提示 JSON 语法是否有效。 | 绿色标识只代表语法通过，不代表满足所有字段要求。 |
| `1:1` | 编辑器光标位置。 | 表示第 1 行、第 1 列，不参与计费。 |
| 复制／格式化 JSON | 复制编辑器内容，或整理缩进与换行。 | 格式化不会修复错误语法。 |
| 模拟请求头 | 为请求头相关计价规则提供测试数据。 | 名称和值按键值对填写；值必须是字符串。 |
| JSON 模式／添加行 | 切换请求头编辑方式，或新增请求头。 | 如 `{"tier":"fast"}`，等同于添加名称为 `tier`、值为 `fast` 的一行。 |
| 模拟时间 | 选择计价规则使用的模拟时刻。 | 支持“当前时间”和“指定时间”。 |
| 指定时间输入框 | 固定测试时刻，便于复现分时价格。 | 必须带时区，如 `2026-09-18T09:00:00+08:00`；`Z` 表示 UTC。 |
| 时间函数时区提示 | 说明时间条件按表达式内的时区判断。 | 输入时间确定时刻，表达式中的时区决定对应的小时、星期等。 |
| 模拟请求费用 | 当前测试条件下的费用结果。 | **包含请求规则，不包含分组倍率**。 |
| 命中档位 | 本次计算使用的计价档位名称。 | 图中的 `base` 是档位名称，不是用户分组。 |

- 请求规则计费与模拟

| 字段或操作 | 填写要求 | 说明 |
| --- | --- | --- |
| 请求体参数 | 参数路径、比较方式和值 | 读取请求 JSON 路径；例如按 `service_tier` 区分服务等级。 |
| 请求头条件 | Header 名称、比较方式和值 | 根据实际发送的请求头匹配。 |
| 时间条件 | 时间项、时区、比较值或范围 | 明确时区，例如 `Asia/Shanghai`。 |
| 规则组倍率 | 填入业务所需倍率 | 同组条件同时满足才命中；多个命中组的倍率相乘，小于 `1` 表示折扣。 |
| Token 估算器 | 输入、输出、缓存和媒体用量 | 检查基础费用和命中档位，不含用户/分组倍率；指定完整输入长度可单独测试阶梯边界。 |
| 模拟请求体 | JSON 对象 | `{}` 表示未提供相应字段。 |
| 模拟请求头 | 键值行或 JSON | 可添加、删除行，用于检验 Header 条件。 |
| 模拟时间 | 当前或指定时间 | 用于验证时间规则，表达式中的时区仍生效。 |
| 模拟请求费用 | 查看结果与命中条件 | 包含请求规则，不含分组倍率；不是已发起上游请求或最终账单。 |

#### 计费表达式部分说明

- 计费用量变量

| 变量     | 名称                 | 定义                                               |
| -------- | -------------------- | ------------------------------------------------------------ |
| `len`    | 完整输入上下文长度   | 用于按长度选择档位。 |
| `p`      | 基础输入 Token       | 按输入基础价计费的用量。 |
| `c`      | 基础输出 Token       | 按输出基础价计费的用量。 |
| `cr`     | 缓存读取 Token       | 命中并复用提示词缓存的输入用量。 |
| `cc`     | 缓存创建 Token       | Claude 的 5 分钟缓存创建用量；其他上游用量语义中为通用缓存创建用量。 |
| `cc1h`   | 1 小时缓存创建 Token | Claude 的 1 小时缓存创建用量。`1h` 是缓存有效期。 |
| `img`    | 图像输入 Token       | 输入图像对应的 Token 用量。 |
| `img_cr` | 图片缓存读取 Token   | 命中缓存的图像输入用量。 |
| `img_o`  | 图像输出 Token       | 模型生成图像对应的输出 Token 用量。 |
| `ai`     | 音频输入 Token       | 输入音频对应的 Token 用量。 |
| `ao`     | 音频输出 Token       | 模型生成音频对应的输出 Token 用量。 |

- 函数与参数定义

| 函数                | 参数定义                                         | 作用／返回值                                                 | 示例                                                         |
| ------------------- | ------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `tier(name, value)` | `name`：档位名称字符串；`value`：计价表达式。    | 返回计价结果，同时记录命中档位名称。                         | `tier("base", p * 2 + c * 8)`                                |
| `fixed(amount)`     | `amount`：有限、非负的数值常量，单位为美元／次。 | 设置按次基础价格，必须直接写在 `tier()` 的第二个参数位置。   | `tier("request", fixed(0.01))`                               |
| `max(a, b)`         | 两个数值。                                       | 返回较大值，可用于设置计算下限。                             | `max(p * 2 + c * 8, 1000)` 对应 Token 表达式费用至少 0.001 美元。 |
| `min(a, b)`         | 两个数值。                                       | 返回较小值，可用于限制某项计算值。                           | `min(p, 100000)` 返回不超过 100,000 的输入用量。             |
| `ceil(x)`           | 一个数值。                                       | 向上取整。                                                   | `ceil(1.2)` 得到 `2`。                                       |
| `floor(x)`          | 一个数值。                                       | 向下取整。                                                   | `floor(1.8)` 得到 `1`。                                      |
| `abs(x)`            | 一个数值。                                       | 返回绝对值。                                                 | `abs(-2)` 得到 `2`。                                         |
| `header(name)`      | `name`：请求头名称字符串。                       | 返回请求头字符串；名称忽略大小写，名称和值去除两端空白；缺失时返回空字符串。 | `header("service-tier") == "priority"`                       |
| `param(path)`       | `path`：请求体字段路径字符串。                   | 返回请求体字段值，保留字符串、数值、布尔值等类型；缺失时为 `nil`。支持点分隔的嵌套路径。 | `param("service_tier") == "priority"`、`param("stream_options.fast_mode") == true` |
| `has(source, text)` | `source`：待检查的值；`text`：查找的子串。       | 将非空值转为文本后进行区分大小写的子串检查；来源为 `nil` 或查找文本为空时返回 `false`。 | `has(header("anthropic-beta"), "fast-mode")`                 |

- 时间函数

| 函数          | 返回值                                | 示例含义                                                 |
| ------------- | ------------------------------------- | -------------------------------------------------------- |
| `hour(tz)`    | 小时，`0–23`。                        | `hour("Asia/Shanghai") >= 21` 表示当地时间 21 点及以后。 |
| `minute(tz)`  | 分钟，`0–59`。                        | `minute("UTC") < 30` 表示当前小时的前 30 分钟。          |
| `weekday(tz)` | 星期，`0–6`；`0` 为周日，`6` 为周六。 | `weekday("Asia/Shanghai") == 0` 表示周日。               |
| `month(tz)`   | 月份，`1–12`。                        | `month("Asia/Shanghai") == 12` 表示 12 月。              |
| `day(tz)`     | 当月日期，`1–31`。                    | `day("Asia/Shanghai") == 1` 表示每月 1 日。              |

- 常用语法

| 语法                             | 含义                         | 示例                                                         |
| -------------------------------- | ---------------------------- | ------------------------------------------------------------ |
| `+`、`-`、`*`、`/`               | 加、减、乘、除。             | `p * 2 + c * 8`                                              |
| `>`、`>=`、`<`、`<=`             | 大小比较，返回真假。         | `len <= 200000`                                              |
| `==`、`!=`                       | 相等、不相等。               | `param("priority") == true`                                  |
| `&&`                             | 逻辑“且”，两个条件都满足。   | `len < 32000 && c < 200`                                     |
| `\|\|`                           | 逻辑“或”，至少一个条件满足。 | `hour("UTC") >= 21 \|\| hour("UTC") < 6`                     |
| `!`                              | 逻辑取反。                   | `!(param("priority") == true)`                               |
| `条件 ? A : B`                   | 条件为真执行 A，否则执行 B。 | `len <= 200000 ? tier("short", p * 2) : tier("long", p * 4)` |
| `(...)`                          | 分组，明确计算顺序。         | `(基础表达式) * (条件 ? 2 : 1)` 是结构示意，需替换其中中文占位内容。 |
| `"文本"`、`true`、`false`、`nil` | 字符串、布尔值与空值。       | `true` 与 `"true"` 不是同一种类型。                          |

<Callout type="info">

- 推荐使用LLM辅助设计提示词，让LLM辅助设计计费表达式：

1. 打开辅助面板；在计价配置中选择“表达式编辑器”，点击“LLM 辅助设计提示词”；展开后可查看内置提示词。
2. 复制提示词；点击面板右侧的“复制提示词”； 出现复制成功提示后，提示词已写入剪贴板；若复制失败，可选中文本手动复制。
3. 粘贴到 LLM；打开 ChatGPT、Claude 等工具，将提示词粘贴到对话输入框；保留内置的语法说明，便于模型按系统支持的格式设计表达式。
4. 补充计价需求；在提示词后追加模型名称、价格、计价方式、档位条件及倍率规则；明确价格单位；若修改已有方案，附上当前表达式并说明需要调整的部分。不要仅凭模型名称让 LLM 猜测价格。
5. 生成表达式；发送完整内容，要求 LLM 返回可直接粘贴的表达式、规则解释和测试样例；对缺少或有歧义的信息，要求先确认，不要自行补全价格和规则。
6. 核对并粘贴；检查价格、变量、条件边界和倍率，将表达式复制回输入框；只粘贴表达式正文，不包含 Markdown 代码围栏、解释文字或测试数据。
7. 验证结果；在 Token 估算器填写用量；涉及请求或时间条件时，再使用“请求模拟”；检查费用和命中档位，并测试阈值前、阈值上、阈值后，以及字段缺失等场景。请求模拟不包含分组倍率。
8. 修正并保存；如果结果不符，将表达式、报错或测试输入、实际结果、期望结果反馈给 LLM；验证通过后在价格配置界面保存；复制提示词或修改模拟样例不会自动保存计价配置。

</Callout>

#### 未设置价格的模型

此列表基于渠道可用模型及当前定价快照，筛选未定价模型。

- 计费与支付-模型定价-未设置价格的模型
![未设置价格的模型](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-models-without-prices.png)

1. 筛选未定价模型并进入价格编辑。
2. 单独配置，或从已核对的源模型复制价格到所选目标。
3. 核对每个目标的协议、缓存和媒体计费差异，再保存。
4. 刷新列表，发起测试请求确认。

#### 工具价格

统一配置搜索、文件检索、图像生成等工具的调用单价，用于计算工具调用附加费用。

- 计费与支付-模型定价-工具价格
![工具价格](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-tool-prices.png)

- 字段或操作说明

| 字段或操作      | 定义                                         | 填写要求或使用说明                                           |
| --------------- | -------------------------------------------- | ------------------------------------------------------------ |
| 工具标识        | 指定价格对应的工具，以及可选的模型匹配范围。 | 通用格式为 `工具名`；模型专属格式为 `工具名:模型前缀*`，例如 `web_search_preview:gpt-4o*`。 |
| 价格（$/1K 次） | 每 1,000 次工具调用的美元单价。              | 填写大于或等于 `0` 的数字，支持小数。例如 `10` 表示每千次 10 美元，单次基础费用为 0.01 美元。 |
| 添加            | 新增一条工具价格规则。                       | 新行的工具标识为空、价格为 `0`。 |
| 删除图标        | 从当前编辑内容中移除该行。                   | 保存后移除对应自定义配置；内置规则缺失时会使用内置兜底价格。 |
| 恢复默认        | 将当前编辑内容替换为内置默认价格列表。       | 会覆盖当前草稿，包括自定义条目。 |
| 切换到 JSON     | 以 JSON 对象形式查看或批量编辑价格。         | 对象键为工具标识，值为非负数字，例如 `{"web_search":10}`。 |
| 切换到可视化    | 返回表格编辑模式。                           | 建议先修复 JSON 错误，再切换模式。                           |
| 复制            | 复制 JSON 编辑区的内容。                     | 在 JSON 模式显示，可用于留存修改前的配置。                   |
| 保存工具价格    | 提交当前完整价格配置。                       | 无效价格或 JSON 错误会阻止保存；提交后应确认成功提示，并重新打开页面核对。 |

- 操作流程

1. 进入配置页面；使用超级管理员账号打开“模型定价”，选择“工具价格”。
2. 选择配置范围；全部模型使用通用工具标识；特定模型系列使用 `工具名:模型前缀*`。
3. 添加或修改规则；修改已有行，或点击“添加”填写工具标识和价格；按美元/千次填写。
4. 检查匹配与金额；按目标模型名称确认最长前缀规则，并换算单次基础费用。
5. 保存配置；点击“保存工具价格”，确认保存成功。
6. 复查配置与计费；重新打开页面核对保存结果，再用受控请求触发目标工具，检查消费记录；核对工具调用次数、单价、分组倍率和最终费用。

<Callout type="info">
单项工具附加费用 = 价格（美元/千次） × 计费调用次数 ÷ 1000 × 分组倍率
总工具附加费用 = 各项工具附加费用之和
</Callout>

#### 上游同步

从预设或上游渠道拉取模型价格，与本地配置对比后，按模型选择价格来源，并通过预览确认批量更新本地计费配置。

- 计费与支付-模型定价-上游同步
![上游价格同步](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-upstream-price-sync.png)

- 字段说明

| 字段 | 定义 | 默认或限制 |
| --- | --- | --- |
| 模型 | 本行价格对应的模型名称。 | 名称可能包含版本后缀，应以完整名称为准。 |
| 当前价格 | 本地用于本次比较的模型计费配置。 | 取自本次加载的本地配置。 |
| 价格来源列 | 展示该来源提供的模型价格，例如“官方定价预设”。 | 由本次拉取结果生成。 |

- 控件操作说明

| 控件操作 | 操作说明 | 限制 |
| --- | --- | --- |
| 选择价格来源 | 点击打开价格来源选择窗口，可选择多个来源及同步端点。 | 只可选择已有的价格来源。 |
| 搜索模型名称 | 输入模型名称或部分关键词。 | 不区分大小写进行包含匹配。 |
| 类型下拉框 | 选择所有类型、表达式计费、按 Token 计费或按请求计费。 | 按来源提供的计费方式筛选。 |

- 计费与支付-模型定价-上游同步-选择价格来源
![选择价格来源](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-upstream-price-sync-select-price-source.png)

- 字段说明

| 字段 | 定义 | 默认或限制 |
| --- | --- | --- |
| 名称 | 预设名称或本地渠道名称。 | - |
| API 地址 | 价格来源的基础地址。 | 使用相对同步路径时与该地址拼接；使用完整 URL 时直接请求该 URL。 |
| 状态 | 渠道状态。 | 已启用、已禁用、自动禁用 |
| 同步端点 | 指定读取价格的接口类型或地址。 | `pricing`、`ration_config`、`OpenRouter`、`custom`。 |

- 控件操作说明

| 控件操作 | 操作说明 | 限制 |
| --- | --- | --- |
| 按名称或 URL 搜索 | 输入关键词筛选匹配的预设或渠道。 | 匹配来源显示名称和 API 地址，不区分大小写。 |
| 行勾选框 | 勾选参与本次价格比较的来源，支持多选。 | 选择比较来源。 |
| 同步端点下拉框 | 根据来源接口选择 `pricing`、`ratio_config`、`OpenRouter` 或 `custom`。 | 来源必须支持所选接口。|
| 自定义端点输入框 | 选择 `custom` 后填写相对路径或完整 URL。相对路径与 API 地址拼接，完整 URL 直接使用。 | 仅在 `custom` 模式显示；完整地址需使用 HTTP/HTTPS。 |
| 应用同步 | 使用选中来源发起价格拉取与比较。 | 仅在 `custom` 模式显示；完整地址需使用 HTTP/HTTPS。 |

- 计费与支付-模型定价-上游同步-应用同步确认
![应用同步确认](/assets/guide/feature-guide/system-settings-billing-and-payments-model-pricing-upstream-price-sync-apply-sync.png)

- 字段说明

| 字段 | 定义 |
| --- | --- |
| 价格来源 | 选中模型的更新价格来源。 |
| 模型 | 将被更新的模型名称。 | 
| 当前价格 | 变更前的本地计费方式及价格。 |
| 更改为 | 本次准备采用的价格或表达式。 |

- 上游价格同步操作流程

1. 进入页面；使用超级管理员账号打开“模型定价 → 上游价格同步”。
2. 选择来源；点击“选择价格来源”，搜索并勾选一个或多个预设或渠道。
3. 配置同步端点；核对 API 地址和同步端点；使用预设时可保留默认值。
4. 拉取价格；点击“确认选择”，等待比较结果。
5. 筛选并对比；通过模型搜索、类型筛选和分页定位目标模型。
6. 选择待同步价格；勾选目标模型对应的来源，或使用来源列头批量勾选。
7. 打开变更预览；检查底部已选数量，点击“应用同步”。
8. 核对并确认；检查“价格来源、模型、当前计费、更改为”，点击“确认更改”。
9. 检查同步结果；确认成功提示，在“模型价格”中复查配置，必要时重新拉取比较。

### 分组定价

配置管理分组的调用倍率、充值倍率、用户可选范围和自动分配顺序，并为指定用户分组配置专属价格与可用分组规则。

- 计费与支付-分组定价
![分组定价](/assets/guide/feature-guide/system-settings-billing-and-payments-group-pricing.png)

#### 定价分组

统一维护分组名称、调用基础倍率、充值倍率及全局默认可选状态，为分组计费和选择提供基础配置。

- 字段说明

| 字段 | 定义 |
| --- | --- |
| 分组名称 | 分组的标识，用于关联用户所属分组、令牌计费分组及相关规则。 |
| 倍率 | 调用按此分组计费时使用的基础倍率，作用于模型计费结果。 |
| 充值倍率 | 根据用户账号所属分组，对支持该配置的在线充值金额计算应用的倍率。 |
| 用户可选 | 是否将该分组纳入全局默认可选的令牌分组范围。 |
| 说明信息 | 用户选择分组时看到的说明文字。 |

- 计费与支付-分组定价-使用教程<br/>
点击使用教程图标打开分组使用教程，了解用户分组、令牌分组、倍率和特殊规则如何共同生效。
![使用教程](/assets/guide/feature-guide/system-settings-billing-and-payments-group-pricing-usage-guide.png)

- 计费与支付-分组定价-分组详情<br/>
点击详情图标打开分组详情，查看基础配置、特殊倍率关系、可见性规则及自动分配位置。
![分组详情](/assets/guide/feature-guide/system-settings-billing-and-payments-group-pricing-group-details.png)

**操作流程**

1. 添加分组，设置名称、调用倍率、充值倍率和可选状态。
2. 在[渠道管理](/zh/docs/guide/feature-guide/admin/channel)配置该组可用的渠道与模型，在[用户管理](/zh/docs/guide/feature-guide/admin/user)设置账户组。
3. 配置下述特殊规则或 Auto 顺序后，点击保存分组比率。
4. 用不同账户组、固定组及 Auto 令牌发起请求，核对实际组、渠道和费用。

#### 特殊倍率规则

为指定用户分组使用指定计费分组的场景设置专属倍率，匹配时替代该计费分组的基础倍率。

- 计费与支付-分组定价-特殊倍率规则-添加用户分组<br/>
点击添加用户分组打开弹窗，选择需要配置专属倍率的用户分组并点击“添加”。
![添加用户分组](/assets/guide/feature-guide/system-settings-billing-and-payments-group-pricing-special-multiplier-rules-add-user-group.png)

- 计费与支付-分组定价-特殊倍率规则-添加倍率覆盖<br/>
点击“ + ”打开添加倍率覆盖弹窗，选择计费分组、填写倍率并点击“添加”。
![添加倍率覆盖](/assets/guide/feature-guide/system-settings-billing-and-payments-group-pricing-special-multiplier-rules-user-group-add-multiplier-override.png)

- 字段说明
| 字段 | 定义 |
| --- | --- |
| 用户分组 | 此组规则适用的账号所属分组。 |
| 计费分组 | 该用户调用时实际使用的目标分组。|
| 倍率 | 指定用户分组使用指定计费分组时采用的特殊倍率。 |

#### 自动分配顺序

设置自动分组模式的全局候选顺序，以及每个令牌可自定义的分组数量上限。

| 字段 | 定义 |
| --- | --- |
| 每个令牌的最大自定义分组数 | 单个令牌自行保存 Auto 分组列表时，允许包含的最大分组数量。 |
| 自动分配列表 | `auto` 令牌继承的全局候选分组及尝试顺序，越靠上越先尝试。 |
| 列表中的分组名称 | 自动路由时可尝试的实际分组。 |

#### 特殊可用分组规则

 按用户所属分组额外开放或隐藏指定的可选令牌分组，实现不同用户分组的差异化可用范围。

| 字段 | 定义 |
| --- | --- |
| 用户分组 | 这些可见性规则适用的账号所属分组。 |
| 规则类型：额外可见 | 将目标分组添加到该用户分组的可选范围，并使用本条规则的说明信息。 |
| 规则类型：隐藏 | 从该用户分组的默认可选范围中移除目标分组。 |
| 分组名称 | 要额外开放或隐藏的分组。 |
| 说明信息 | 对指定用户分组展示的目标分组说明。 |

#### 默认使用自动分组开关

控制新建令牌在用户具备相应权限时是否默认采用自动分组模式，并继承全局自动分配顺序。

### 支付网关

统一配置在线充值的支付渠道、充值金额选项、优惠规则和支付通知，使用户能够通过已接入的支付平台付款并获得账户额度。支持接入EPay、Stripe、Creem、Waffo Pancake、Waffo等支付网关。

#### 支付合规确认

由超级管理员确认运营合规声明，解除支付、兑换码、订阅套餐及邀请奖励相关功能的合规限制。确认内容涉及上游授权、服务使用与商业化范围、适用的运营义务及责任，完整条款以确认弹窗为准。

- **操作流程**

1. 使用超级管理员账号进入「计费与支付」 → 「支付网关」。
2. 点击「确认合规」，阅读条款并按提示填写各段确认文字。
3. 点击「确认并启用」，等待提交成功。
4. 确认页面显示「合规已确认」及确认用户、时间，再继续配置支付渠道。

<Callout type="info">
支付网关配置、充值、支付等相关功能使用前，需要先完成支付合规确认。
</Callout>

#### 常规设置

常规设置为对所有支付网关的共享配置，包括以下四个配置区域：
1. **通用设置**；设置每美元余额（Epay）的收费金额，用户可以充值的最小美元金额。
2. **付款方式**；配置钱包中的支付按钮，并指定每个按钮使用的支付流程。
3. **充值金额选项**；提供用户可以直接选择的常用充值金额。
4. **金额折扣**；为指定充值金额配置折扣率。

- 计费与支付-支付网关-常规设置
![支付网关常规设置](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-general.png)

- 计费与支付-支付网关-添加支付方式
![添加常规支付方式](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-general-payment-methods-add-method.png)

- 计费与支付-支付网关-添加金额优惠
![添加金额优惠](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-general-amount-discounts-add-discount-tier.png)

- **字段说明**

| 字段 | 用途说明 | 填写要求 | 默认值 / 示例 |
| --- | --- | --- | --- |
| 价格（本地货币/美元） | 每美元余额（Epay）的收费金额。 | ≥ `0`；按服务商收款币种和定价填写。 | 默认：`7.3`。 |
| 最低充值（美元） | 设置 Epay 允许的最低充值金额。 | ≥ `0`；并满足服务商最低收款要求。 | 默认：`1`。 |
| 名称 | 用户看到的支付方式名称。 | 弹窗必填，填写支付方式的显示名称。 | 示例：支付宝。 |
| 支付处理标识 | 决定该入口调用的支付流程。 | 必填；标识须为服务商支持的 Epay 支付类型。 | 示例：`alipay`、`wxpay`。 |
| 图标 | 为按钮显示对应的图标。 | 输入 react-icons 组件名。无法解析的名称不会显示图标。 | 示例：`SiAlipay`、`SiWechat`。 |
| 最低充值 | 为单个支付按钮设置额外最低金额。 | 选填。 | 示例：`50`。 |
| 充值金额选项 | 钱包中的预设快捷充值金额列表。 | 按整数配置充值数量。 | 默认：`[10,20,50,100,200,500]`。 |
| 添加新金额 | 向快捷列表添加一个金额。 | > `0`，使用整数。 | 示例：`1000`。 |
| 充值金额（USD） | 指定哪一个充值金额使用该折扣。 | 正整数，按请求金额精确匹配。 | 示例：`100`。 |
| 折扣率 | 应付金额乘以的系数。 | `0` < 折扣率 ≤ `1` 。 | 示例：`0.85`，即支付 85%。 |
| 优惠 | 显示折扣对应的减免比例。 | 按折扣率自动计算。 | 示例：折扣率 `0.85` 对应优惠 `15%`。 |

<Callout type="info">
- 金额计算说明：<br/>
`应付金额 = 充值金额数量 × 价格（本地货币/美元） × 用户分组充值倍率 × 匹配的折扣率`<br/>
例如 Epay 充值 `100` 美元余额，价格（本地货币/美元）为 `7.3`、发起充值用户的分组充值倍率为 `1`，且配置 `100 → 0.85`，则应付 `620.50` 单位本地货币，到账为 `100` 美元余额。
</Callout>

#### EPay

接入 Epay 国内聚合支付平台，支持支付宝、微信支付等，并根据支付通知自动完成充值到账。

- 计费与支付-支付网关-EPay
![EPay 网关](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-epay.png)

- **字段说明**

| 字段 | 用途说明 | 填写要求 |
| --- | --- | --- |
| Epay 端点 | 连接支付服务商并发起支付请求。 | 必填；您的 Epay 服务提供的基础地址，例如 `https://pay.example.com`。 |
| 回调地址 | 设置支付通知使用的站点基础地址。 | 选填；只填写站点根域名，例如 `https://api.example.com`。不要填写 /api/user/epay/notify 这类路径。留空则使用服务器地址。 |
| 易支付商户 ID | 标识当前收款商户。 | 接入时必填，与端点、密钥对应。 |
| Epay 密钥 | 用于请求签名和支付结果校验。 | 首次接入时填写；已保存的密钥不回显，更新配置时留空保留原值。 |

- **操作流程**

1. 填写端点、商户 ID 和密钥。
2. 核对回调基础地址，系统会在其后拼接 `/api/user/epay/notify`。
3. 统一保存后，在钱包发起测试充值。

<Callout type="warn">
**Epay 安全提醒**：<br/>
Epay 是一种支付协议，不是某个固定官网。请自行核验服务提供方，不要轻信他人搭建的第三方 Epay 站点。
</Callout>

#### Stripe

接入 Stripe 国际信用卡支付平台，并通过 Webhook 接收支付结果。

- 计费与支付-支付网关-Stripe
![Stripe 网关](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-stripe.png)

- **字段说明**

| 字段 | 用途说明 | 填写要求 | 默认值 / 示例 |
| --- | --- | --- | --- |
| API 密钥 | 授权 New API 调用 Stripe。 | 首次接入时填写具有所需权限的服务端密钥；接受 `sk_` 或 `rk_` 前缀。（除非更新，否则留空）。 | 默认：空。 |
| Webhook 密钥 | 校验 Stripe 支付通知的签名。 | 从对应 Webhook 配置取得；（除非更新，否则留空）。 | 默认：空；格式示例：`whsec_...`。 |
| 价格 ID | 指定使用的 Stripe 产品价格。 | Stripe 产品价格 ID。 | 默认：空；格式示例：`price_...`。 |
| 单价（本地货币 / USD） | 计算钱包中显示的应付金额预览。 | 按 Stripe 实际价格设置预览单价。 | 默认：`8`。 |
| 最低充值（美元） | 设置 Stripe 的最低充值数量。 | 正整数。 | 默认：`1`。 |
| 促销代码 | 允许用户在 Stripe 输入优惠代码。 | 需要此能力时开启。 | 默认：关闭。 |

- **操作流程**

1. 在 Stripe 后台添加页面提供的 Webhook 地址。
2. 配置 Webhook 接收支付状态通知。
3. 填写 Stripe 的 API 密钥、Webhook 密钥和价格 ID。
4. 设置充值单价、最低充值金额，并按需开启促销代码。
5. 点击“保存所有设置”，统一保存后测试充值。

<Callout type="info">
Stripe 需要配置 Webhook 接收支付状态通知，Webhook URL 为：`https://your-domain.com/api/payment/stripe/webhook`。
</Callout>

#### Creem

接入 Creem 国际支付平台，将 Creem 产品映射为充值套餐，购买成功后按配置的配额点数入账。

- 计费与支付-支付网关-Creem
![Creem 网关](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-creem.png)

- **字段说明**

| 字段 | 用途说明 | 填写要求 | 默认值 / 示例 |
| --- | --- | --- | --- |
| API 密钥 | 调用 Creem 创建结账会话。 | Creem API 密钥（除非更新，否则留空）。 | 默认：空。 |
| Webhook 密钥 | 校验 Creem 支付通知签名。 | Webhook 签名密钥（除非更新，否则留空）。 | 默认：空。 |
| 测试模式 | 选择使用测试接口还是正式接口。 | 开启时使用测试环境的密钥和产品，关闭时使用正式配置。 | 默认：关闭。 |

**添加产品**

- 计费与支付-支付网关-Creem-添加 Creem 产品
![添加 Creem 产品](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-creem-add-product.png)

- **字段说明**

| 字段 | 用途说明 | 填写要求 | 默认值 / 示例 |
| --- | --- | --- | --- |
| 产品名称 / 名称 | 向用户展示充值套餐名称。 | 弹窗必填，填写充值套餐名称。 | 示例：基础充值套餐。 |
| 产品 ID | 关联 Creem 后台产品。 | 弹窗必填，从您的 Creem 仪表板获取 Creem 产品 ID。 | 格式示例：`prod_...`。 |
| 货币 | 显示本地配置的产品币种。 | 可选 `USD` 或 `EUR`，应与 Creem 后台一致。 | 新增默认：`USD`。 |
| 价格 | 在本地展示、记录产品售价。 | 弹窗要求至少 `0.01`。 | 新增初始：`0`（需修改）；示例：`10`。 |
| 额度 | 添加到用户账户的配额点数数量。 | 按整数填写，最小为 `1`。 | 新增初始：`0`（需修改）；示例：`5000000`。 |

- **操作流程**

1. 配置 Creem Webhook，并填写 API 密钥和通知密钥，选择测试或正式模式。
2. 点击“添加产品”，填入产品 ID、名称、货币和价格。
3. 将期望到账余额换算为配额点数，填写“额度”。
4. 核对本地售价、币种与 Creem 产品一致，统一保存后到钱包选择产品测试。

#### Waffo Pancake

绑定 Pancake 商户、店铺和充值产品，按用户充值金额创建结账会话。

- 计费与支付-支付网关-Waffo Pancake
![Waffo Pancake MoR](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-waffo-pancake-mor.png)

| 页面字段或状态 | 填写要求 | 说明 |
| --- | --- | --- |
| Merchant ID | 提供方分配的商户 ID | 不是截图占位值；更换商户时同步核对私钥与目录归属。 |
| API Private Key | 对应商户的 RSA 私钥 | 不回显；保存时空白保留旧值。该文本框可显示新输入私钥，截图前务必清除敏感内容。 |
| Return URL | 新产品的支付成功返回地址 | 创建产品时写入 SuccessURL；之后只修改本地配置不会更新已有产品返回地址。 |
| Store | 目录加载后选择 | 商店绑定，切换商店后需重新选择产品。 |
| Product | 选择有效的一次性产品 | 钱包充值使用该产品并在 checkout 中设置本次价格，不必为每个充值金额创建 SKU。 |
| 创建商店和产品 | 确认外部写入后执行 | 会实际创建并发布资源，不是本地预览。 |
| 已保存绑定 | 保存后核对 | 自动预选、当前选择和已保存绑定是不同状态。 |

- **操作流程**

1. 输入完整商户 ID 和私钥后，页面自动验证并加载目录；已有凭证可在打开页面时使用。不要寻找不存在的独立“测试连接”按钮。
2. 选择已有商店和产品；如果确实需要新资源，先填写返回地址，再执行创建商店和产品。
3. 核对当前商户、商店及产品，保存所有设置，确认已保存绑定。
4. 在提供方分别配置 `<ServerAddress>/api/waffo-pancake/webhook/test` 和 `/api/waffo-pancake/webhook/prod`。
5. 使用专门测试账户验证 checkout、签名通知和余额；再按生产商户要求检查正式配置。

**注意事项**

- 目录可能自动选中首个可用产品，不能把预选理解为旧绑定已修复。保存只检查必要字段，不替代产品归属、发布状态和环境验证。
- 创建、发布有真实外部副作用，取消、刷新或本地保存失败都不会撤销资源。部分创建失败时先检查提供方后台，避免重复创建。
- `test`、`prod` 路径用于检查通知 mode 与路径一致，不保证独立账本。不要让生产用户参与测试支付。
- 钱包产品与[订阅管理](/zh/docs/guide/feature-guide/admin/subscription)的套餐产品不同；套餐使用独立产品，但仍需满足共用 Webhook 的全局配置条件。
- 新私钥用于目录请求时可能出现在请求 URL 中，排错时不要公开完整 URL、代理访问日志或浏览器网络记录。保存成功不代表已完成支付验收。

#### Waffo

先选择沙盒或生产环境，再填写该环境的完整凭证。仅开启开关并不意味着网关可用。

| 页面字段 | 默认或填写要求 | 说明 |
| --- | --- | --- |
| 启用 Waffo | 默认关闭 | 钱包入口与回调还要求合规和当前环境的 API Key、RSA 私钥、平台公钥。 |
| 沙盒模式 | 默认关闭 | 开启后使用沙盒凭证及服务地址，不能混用生产三件套。 |
| API Key / 沙盒 API Key | 默认空 | 本页空输入保留已有值，轮换时填写新值。 |
| RSA Private Key / 沙盒私钥 | 默认空 | SDK 使用 Base64 DER PKCS#8 RSA 私钥；不要直接将任意 PEM 文本当成兼容格式。空输入保留。 |
| Waffo Public Key / 沙盒公钥 | 默认空 | 平台验签公钥，使用 Base64 X.509 SPKI RSA 格式；可回显，清空保存会移除，不适用私钥的留空保留规则。 |
| 商户 ID | 服务商提供 | `WaffoMerchantId`，核对与当前凭证的账户关系。 |
| 货币 | 只读，可能显示空 | 下单未配置时回退 USD，不跟随系统显示货币自动切换。 |
| 单价 | 默认 `1` | `WaffoUnitPrice`，结合充值值、账户组充值倍率和精确匹配优惠计算；不使用显示汇率自动换算。 |
| 最低充值 | 默认 `1`；填写正整数 | `WaffoMinTopUp`，避免使用表单允许但后端按整数解析的小数。 |
| 通知地址 | 可选 | `WaffoNotifyUrl`，填写完整通知地址；为空时按回调站点拼接 `/api/waffo/webhook`。 |
| 返回地址 | 可选 | `WaffoReturnUrl`，为空时返回 `<ServerAddress>/wallet?show_history=true`，不负责入账。 |

- 计费与支付-支付网关-Waffo
![Waffo 网关](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-waffo.png)

**新增支付方式**

- 计费与支付-支付网关-Waffo-新增 Waffo 支付方式
![新增 Waffo 支付方式](/assets/guide/feature-guide/system-settings-billing-and-payments-payment-gateway-waffo-add-payment-method.png)

| 弹窗字段 | 填写要求 | 说明 |
| --- | --- | --- |
| 显示名称 | 必填 | 用户看到的按钮名称。 |
| 图标 | 可选；上传不超过 100 KiB | 支持选择 PNG、JPG、SVG、WebP；建议尺寸不超过 128×128，图标不决定路由。 |
| 支付方式类型 | 按服务商要求填写 | 例如 `CREDITCARD,DEBITCARD`，对应 `payMethodType`，不是常规支付方式的 `type`。 |
| 支付方式名称 | 可选的网关方法标识 | 对应 `payMethodName`，如具体钱包方式；不同于显示名称。 |

1. 开启 Waffo，选择环境并填写对应三项凭证、商户资料和金额设置。
2. 核对通知和返回地址，新增或编辑支付方式；弹窗确认仅更新列表。
3. 保存所有设置，再用测试账户选择相应方式下单。
4. 核对 `X-SIGNATURE` 验签、订单成功状态、到账额度及日志。支付页面返回不代表入账成功。

通知地址优先级为：`WaffoNotifyUrl` → `CustomCallbackAddress + /api/waffo/webhook` → `ServerAddress + /api/waffo/webhook`。返回地址独立处理。存在待结订单时不要随意关闭网关或切换验签环境，否则正常通知也可能无法处理。

#### 金额与入账核对

| 网关 | 配置时重点核对 |
| --- | --- |
| EPay | 本地单价、充值倍率、优惠与服务商实收；到账基于订单充值额度。 |
| Stripe | 本地报价不等于 Stripe Price 实收；分组倍率参与站内入账，优惠码和本地优惠不是同一机制。 |
| Creem | 产品实际价格与站内展示一致；配额点数 直接作为内部额度入账。 |
| Waffo | 环境、实际货币、单价、倍率和通知地址；不要用显示汇率推断网关金额。 |
| Pancake | 钱包 checkout 使用本次 USD 价格；提供方税费及最终应付需在 checkout 核对。 |

签名正确、订单匹配和状态允许后才会入账；重复成功通知不会重复发放同一订单额度。不要通过刷新成功页面、重复发起付款或盲目补单处理回调故障。TOKENS 兼容模式存在换算、整数取整及限额差异，不要承诺任意 Token 数量都能精确购入；有待结订单时不要修改额度换算基准。

### 签到奖励

配置每日签到功能开关及随机奖励额度上下限，用户签到成功后获得相应额度。

**参数和控件说明**
| 字段 | 定义 |
| ---  | --- |
| 启用签到功能 | 控制站点是否允许用户通过每日签到领取随机额度奖励。 |
| 签到最小额度 | 单次签到奖励的下限，使用系统内部额度单位。 |
| 签到最大额度 | 单次签到奖励的上限，使用系统内部额度单位。 |

**操作流程**
1. 开启签到功能，填写相应参数。
2. 点击保存签到设置，刷新确认。
3. 用测试用户签到，核对奖励和系统日志。

- 计费与支付-签到奖励
![签到奖励](/assets/guide/feature-guide/system-settings-billing-and-payments-check-in-rewards.png)

**注意事项**

- 截图展示关闭状态，因此没有奖励输入框；并非缺少奖励配置能力。
- 最大值大于最小值时，在包含两端的区间内随机发放；否则固定发放最小值。当前表单不会因最小值大于最大值自动报错，保存前自行核对。
- 每日按服务进程的本地日期判断，不按每位用户的浏览器时区判断；多实例应保持时区一致。
- 用户和日期的唯一约束防止同一天重复领取；开启全局 Turnstile 时，签到请求也需通过对应校验。

## 保存后的验证清单

- [ ] 刷新设置，核对变更字段；模型价格遇到版本冲突时重新加载，普通设置检查是否部分成功。
- [ ] 用已知 Token、缓存、媒体和工具用量验证估算及真实日志；不要把估算器当真实账单。
- [ ] 用不同账户组和令牌组验证实际渠道、分组倍率及 Auto 顺序。
- [ ] 比较同一额度在钱包和日志中的显示，确认未混淆显示汇率与支付单价。
- [ ] 每个网关分别测试下单、实际收费、签名通知、订单状态与到账额度；确认返回页不是唯一成功依据。
- [ ] 测试新用户、邀请注册、邀请额度转移及重复签到，确认奖励进入正确余额。

## 常见问题

- **保存成功但价格没变？** 区分编辑草稿与最终保存，检查模型名、当前模式、提供方覆盖及请求开始时的价格快照。
- **关闭某项价格是否就是免费？** 不一定。移除覆盖可能恢复默认；工具价格还可能匹配更具体前缀。显式零值、未配置和删除是不同状态。
- **公开价格接口为什么访问失败？** 检查公开倍率开关，当前路径为 `/api/ratio_config`。
- **100 的优惠为什么没有用于 200？** 金额优惠按原始金额精确匹配，不是最低阈值规则。
- **Stripe 报价与付款页不一致？** 核对 Stripe Price 的货币、单价和数量，以及本地单价、充值倍率、优惠与 Stripe 优惠码的区别。
- **Creem 产品额度填 1 为什么不是到账 1 美元？** 该字段是内部 配额点数；默认基准下系统 1 USD 对应 500000 quota。
- **密钥框为空是否未配置？** 不一定。多数 API 密钥和私钥不回显；各字段留空规则不同，尤其不能将 Waffo 公钥当成留空保留的私钥。
- **用户不能选择某个组？** 核对公共可用列表、特殊规则、账户自身组、倍率配置及令牌限制；使用 Auto 还需开放 `auto`。
- **Pancake 创建成功为何没有完成配置？** 外部资源创建、当前选择和本地绑定保存是三个阶段，需核对并保存绑定。
- **签到每天都是最小值？** 检查最大值是否不大于最小值，并确认输入使用的是 配额点数。

## 相关文档

- [系统设置概览](./system-setting)：配置入口与生效机制。
- [模型管理](/zh/docs/guide/feature-guide/admin/model)：模型目录与元信息。
- [渠道管理](/zh/docs/guide/feature-guide/admin/channel)：渠道模型、分组与路由。
- [用户管理](/zh/docs/guide/feature-guide/admin/user)：用户分组和账户维护。
- [钱包与订阅](/zh/docs/guide/feature-guide/user/wallet)：用户充值、余额与套餐使用。
- [订阅管理](/zh/docs/guide/feature-guide/admin/subscription)：套餐及独立支付产品。
- [日志与异步任务](/zh/docs/guide/feature-guide/user/log)：请求用量、扣费和任务状态。
- [模型与路由配置](./settings-models)：重试、Auto 相关运行条件与供应商适配。

</div>
