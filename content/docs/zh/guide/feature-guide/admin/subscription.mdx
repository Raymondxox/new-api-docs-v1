---
title: 订阅管理
description: 创建和维护订阅套餐，配置有效期、额度周期、分组权益与支付方式，并管理套餐的发布状态。
---

import { Callout } from 'fumadocs-ui/components/callout';

订阅套餐把价格、有效期、可用额度、额度重置周期、用户分组和支付方式组合成一项可购买的权益。管理员从侧边栏进入「订阅」，或访问 `/subscriptions`，可以创建、编辑、启用、禁用套餐，以及批量重置某个套餐下的有效订阅额度。

页面和订阅管理接口要求 Admin 或 Root 权限。创建、编辑、启用或禁用套餐，以及在用户管理中添加订阅，还要求 Root 已确认当前版本的支付合规条款。合规确认入口和支付网关配置说明参阅[计费与支付配置](/zh/docs/guide/feature-guide/system-settings/settings-billing)。

<Callout type="info">
  支付合规条款只能由 Root 确认。条款确认后，具有 Admin 或 Root 权限的账户才可以执行受保护的套餐操作；未确认时，控制台会禁用新建、编辑、启停和套餐级额度重置等操作。
</Callout>

## 页面概览

{/* 截图位置：订阅管理页面.png；展示套餐列表、列表字段和三个行操作。 */}

订阅管理列表同时显示启用和禁用的套餐。后端按「排序值降序、ID 降序」返回数据：排序值越大越靠前；排序值相同时，较新的套餐通常排在前面。

| 列 | 说明 |
| --- | --- |
| ID | 套餐的唯一编号。排查订单、订阅或接口问题时，可用它确认关联的套餐。 |
| 套餐 | 显示套餐标题和副标题。 |
| 价格 | 显示用户购买套餐需要支付的金额。列表当前以 `$` 格式显示。 |
| 有效期 | 显示每份新订阅从创建时开始计算的有效时长。 |
| 额度重置 | 显示不重置、每日、每周、每月或自定义周期。 |
| 优先级 | 对应编辑表单中的「排序」值，只控制套餐展示顺序，不是模型或渠道路由优先级。 |
| 状态 | 启用表示可进入用户侧公开购买流程；禁用表示不再向用户展示。 |
| 支付渠道 | 显示套餐已填写的 Stripe、Creem 或 Waffo Pancake 产品标识。该列不显示钱包余额和 EPay，也不能单独证明支付网关已经可用。 |
| 套餐额度 | 显示每份订阅包含的总额度；`0` 显示为「无限制」。 |
| 升级分组 | 显示订阅创建后要切换到的用户分组；未配置时显示「不升级」。 |
| 操作 | 依次提供编辑、重置订阅额度，以及启用或禁用操作。当前页面不提供删除套餐的入口。 |

## 使用前准备

创建套餐前，建议先完成以下检查：

1. 由 Root 在「系统设置 → 计费与支付 → 支付网关」确认支付合规条款。
2. 明确套餐面向的用户、售价、有效期、总额度和额度重置方式。
3. 如需改变用户分组，先在分组配置中创建并核对升级分组和降级分组。
4. 如需第三方支付，先完成对应网关的全局凭证、回调地址和 Webhook 配置。
5. 决定是否允许钱包余额兑换，以及订阅额度耗尽后是否允许继续使用钱包余额。

<Callout type="warn">
  套餐保存成功不等于所有支付方式已经可用。第三方支付同时依赖套餐产品 ID、全局网关配置、回调可达性和支付平台中的产品状态；发布前应使用测试账户完成一次端到端购买验证。
</Callout>

## 创建套餐

{/* 截图位置：订阅管理-新建套餐.png；展示新建套餐抽屉中的基本信息、有效期、额度重置和第三方支付配置。 */}

1. 在订阅管理页面点击「新建套餐」。
2. 在「基本信息」中填写套餐标题、价格、额度、分组、限购和排序等字段。
3. 设置套餐是否立即启用、是否允许余额兑换，以及额度用尽后是否允许使用钱包余额。
4. 在「有效期设置」中选择有效期单位和值。
5. 在「额度重置」中选择重置周期；选择自定义周期时填写秒数。
6. 如需 Stripe、Creem 或 Waffo Pancake 支付，填写或选择对应的产品标识。
7. 点击「保存更改」。保存成功后，套餐状态由「启用状态」开关决定；新建表单默认启用。
8. 返回列表核对价格、有效期、重置周期、状态、套餐额度和升级分组，再到用户侧检查展示与购买流程。

### 基本信息参数

{/* 截图位置：订阅套餐-新建或编辑套餐中的升级分组和降级分组选项.png；展示升级分组和降级分组的可选值。 */}

| 界面字段 | API 字段 | 类型、默认值与范围 | 生效方式与注意事项 |
| --- | --- | --- | --- |
| 套餐标题 | `title` | 必填文本；新建时为空。 | 显示在管理列表和用户侧套餐卡片中。只填写空白字符会被拒绝。 |
| 套餐副标题 | `subtitle` | 可选文本；新建时为空。 | 用于补充适用场景或权益摘要，不参与计费。 |
| 套餐价格 | `price_amount` | 数值；默认 `0`；后端允许 `0–9999`。 | 表示用户购买一次套餐需要支付的金额。套餐记录的 `currency` 固定保存为 `USD`，具体支付币种和结算行为仍受网关配置影响。 |
| 额度 | `total_amount` | 非负数；默认 `0`；`0` 表示不限总额度。 | 界面显示单位跟随实例的计费显示设置，保存时会换算为内部 quota。该额度会在创建用户订阅时写入权益快照。 |
| 升级分组 | `upgrade_group` | 可选；默认「不升级」；选项来自当前分组配置。 | 创建订阅时把用户切换到目标分组，并保存购买前分组。目标分组不存在时无法保存。 |
| 降级分组 | `downgrade_group` | 可选；默认「降级到购买前分组」；选项来自当前分组配置。 | 订阅到期、失效或删除时优先切换到指定分组；留空时按条件尝试恢复购买前分组。 |
| 限购 | `max_purchase_per_user` | 非负整数；默认 `0`；`0` 表示不限。 | 统计该用户在此套餐下仍存在的全部订阅记录，包括已过期、已取消和管理员添加的记录。删除记录后才会释放对应次数。 |
| 排序 | `sort_order` | 整数；默认 `0`。 | 数值越大，套餐在管理列表和用户侧越靠前；列表中显示为「优先级」。 |
| 启用状态 | `enabled` | 开关；默认开启。 | 控制套餐是否进入公开展示和普通购买流程，不会终止已经创建的订阅。 |
| 允许余额兑换 | `allow_balance_pay` | 开关；默认开启。 | 开启后，余额足够的用户可以用钱包余额购买；关闭后不显示该购买方式。 |
| 额度用尽后允许使用钱包余额 | `allow_wallet_overflow` | 开关；默认开启。 | 创建订阅时写入权益快照。是否实际回退钱包还取决于用户选择的计费偏好。 |

<Callout type="info">
  界面中的「额度」不是所有部署都固定显示为美元。其标签和步进值会跟随实例的货币或 Token 显示设置，最终保存为 New API 内部 quota；配置和核对时应使用当前实例显示的单位。
</Callout>

### 有效期参数

| 界面字段 | API 字段 | 可选值与默认值 | 计算方式 |
| --- | --- | --- | --- |
| 有效期单位 | `duration_unit` | 年、月、日、小时、自定义秒数；默认「月」。 | 决定如何从订阅创建时间计算结束时间。 |
| 有效期数值 | `duration_value` | 正整数；默认 `1`；选择自定义秒数时不使用。 | 年和月按日历增加；日按 24 小时、小时按固定小时数增加。 |
| 自定义秒数 | `custom_seconds` | 选择自定义有效期时必须大于 `0`。 | 直接把指定秒数加到订阅创建时间。非正值会导致订阅无法创建。 |

有效期在每份用户订阅创建时计算并保存。之后修改套餐的有效期，不会延长或缩短已经存在的订阅。

### 额度重置参数

| 界面字段 | API 字段 | 可选值与默认值 | 生效方式 |
| --- | --- | --- | --- |
| 重置周期 | `quota_reset_period` | 不重置、每日、每周、每月、自定义秒数；默认「不重置」。 | 决定订阅的下一次额度重置时间。每日推进到下一天，按周推进到下一个周一，按月推进到下月第一天。 |
| 自定义秒数 | `quota_reset_custom_seconds` | 仅选择自定义周期时使用，必须大于 `0`。 | 从当前计算基准向后增加指定秒数。下一次重置晚于订阅结束时间时，不再安排重置。 |

重置额度只会把已用额度清零，不会改变套餐总额度，也不会延长订阅有效期。

### 第三方支付参数

| 界面字段 | API 字段 | 套餐侧用途 | 权限与依赖 |
| --- | --- | --- | --- |
| Stripe Price ID | `stripe_price_id` | 将套餐关联到 Stripe Price，并用 Stripe Checkout 的 `subscription` 模式创建首次购买会话。 | 还需配置全局 Stripe API Secret、Webhook Secret 和充值用 Stripe Price ID；套餐 Price ID 与系统充值 Price ID 是两个独立配置。套餐 Price 必须适用于订阅模式。 |
| Creem Product ID | `creem_product_id` | 将套餐关联到 Creem 产品。 | 还需配置全局 Creem API Key、Webhook Secret 和产品列表；套餐 Product ID 与系统充值产品配置是两个层级。 |
| Waffo Pancake Product ID | `waffo_pancake_product_id` | 从已保存店铺的一次性产品中选择套餐产品。 | 产品列表和「+ 新建」接口要求 Root，并依赖 Merchant ID、Private Key、Store ID 和回调配置。当前网关启用判定还要求系统已配置钱包充值用 Pancake Product ID；它与套餐 Product ID 不能混用。 |

EPay 没有套餐级产品 ID。启用全局 EPay、配置至少一种支付方式且套餐价格不低于 `0.01` 后，用户可以按套餐价格创建 EPay 订单。钱包余额购买同样不需要第三方产品 ID。

<Callout type="warn">
  Stripe 和 Creem 下单时分别以套餐的外部 Price ID 或 Product ID 为准，实际扣款金额与币种由第三方产品决定；New API 列表展示和订单记录使用本地「套餐价格」。发布前必须确保外部产品与本地价格完全一致。修改一端不会自动同步另一端。
</Callout>

#### 创建 Waffo Pancake 产品

「Waffo Pancake Product ID」右侧的「+ 新建」会使用当前表单中的套餐标题和价格，在已保存的 Pancake 店铺中创建并发布一个一次性产品，然后把返回的产品 ID 填入表单。

执行前需要满足：

- 当前账户是 Root；
- 已在计费与支付设置中完成 Waffo Pancake 凭证和店铺配置；
- 套餐标题不为空；
- 套餐价格大于 `0`。

该操作会先在第三方平台创建产品，之后仍需保存套餐表单。若第三方产品创建成功但套餐保存失败，Pancake 中的产品不会由 New API 自动删除，应到第三方平台核对并按需清理。

## 编辑套餐

{/* 截图位置：订阅管理-编辑套餐.png；展示编辑已有套餐时回填的字段和当前配置。 */}

1. 在目标套餐右侧点击编辑图标。
2. 核对抽屉中回填的基本信息、有效期、重置周期和产品 ID。
3. 修改需要调整的字段并点击「保存更改」。
4. 返回列表确认新值；如涉及支付、分组或重置周期，再完成相应的购买或额度重置验证。

编辑套餐主要影响后续购买和新建订阅，不是批量迁移既有订阅的工具。各类字段对已有订阅的影响如下：

| 套餐字段 | 对已有订阅的影响 |
| --- | --- |
| 价格、允许余额兑换、启用状态、排序、支付产品 ID | 影响后续展示或购买，不改变已创建订阅的权益。 |
| 有效期、总额度、升级分组、降级分组、钱包溢出 | 创建订阅时写入快照；修改套餐不会重写已有订阅中的对应值。 |
| 额度重置周期、自定义重置秒数 | 自动重置或管理员选择推进下次重置时间时，会读取套餐当前规则，因此可能影响已有的有效订阅。 |
| 限购 | 后续购买或管理员添加订阅时重新统计现存记录并应用当前上限。 |

<Callout type="warn">
  待支付订单保存的是套餐 ID 和下单金额。支付回调完成订单时会重新读取套餐并创建权益；用户支付期间若修改套餐的有效期、额度、分组或钱包溢出配置，最终创建的权益可能与下单时看到的内容不同。高风险调整应先暂停公开购买并处理待支付订单。
</Callout>

## 启用和禁用套餐

{/* 截图位置：订阅管理-禁用.png；展示禁用套餐的二次确认提示。 */}

在套餐行末点击状态操作图标，可以启用或禁用套餐。系统会显示二次确认：

- **启用**：套餐重新进入用户侧公开展示和普通购买流程。
- **禁用**：套餐不再向用户显示，也不能发起新的普通购买。

禁用套餐不会：

- 取消或暂停已经创建的用户订阅；
- 清零订阅额度或改变结束时间；
- 使历史订单失效；
- 阻止已经创建的待支付订单在成功回调后完成；
- 阻止管理员在用户管理中为指定用户添加该套餐。

<Callout type="warn">
  当前订阅管理页面没有删除套餐功能。禁用是发布状态控制，不是撤销权益；如需立即终止某个用户的订阅，应在「用户管理 → 管理订阅」中执行失效操作，并先确认分组回退和其他有效订阅的影响。
</Callout>

## 重置订阅额度

{/* 截图位置：订阅管理-重置订阅额度.png；展示套餐级额度重置和「推进下次重置时间」开关。 */}

套餐列表中的重置操作会处理该套餐下所有「状态为 `active` 且尚未到期」的订阅：

1. 点击目标套餐右侧的重置图标。
2. 确认对话框中的套餐名称。
3. 决定是否开启「推进下次重置时间」。该开关默认开启。
4. 点击「重置额度」。成功提示会显示实际重置的有效订阅数量。

| 选项 | 实际行为 |
| --- | --- |
| 开启「推进下次重置时间」 | 清零已用额度，并按套餐当前重置规则重新计算下一次重置时间。 |
| 关闭「推进下次重置时间」 | 只清零已用额度，保留订阅原有的下一次重置时间。 |

没有匹配的有效订阅时，套餐级操作可以成功完成并显示重置 `0` 条。该操作不会创建新订阅、延长有效期、修改总额度、返还钱包余额或删除历史记录。

<Callout type="warn">
  这是对套餐下所有用户的批量操作。执行前应确认目标套餐、重置周期和影响人数；尤其不要把它当作给单个用户补额度的快捷方式。单个用户的重置应从「用户管理 → 管理订阅」执行。
</Callout>

## 用户订阅与权益状态

订阅管理页面维护的是「套餐定义」。某个用户已经获得的订阅，需要在「用户管理」中打开该用户的「管理订阅」进行维护。管理员可以在那里添加订阅、按套餐重置额度、使订阅失效或删除订阅记录。

| 用户订阅状态 | 含义 |
| --- | --- |
| `active` | 订阅处于有效期内，可以按计费偏好参与额度结算。 |
| `expired` | 到达结束时间后，被后台维护流程标记为已过期。 |
| `cancelled` | 管理员提前执行失效，结束时间同时改为当前时间。 |

「失效」会保留订阅历史记录；「删除」会硬删除该条订阅记录且没有恢复入口。删除订阅并不表示相关支付订单、充值流水、使用日志或任务记录也会被一并删除。完整操作说明参阅[用户管理](/zh/docs/guide/feature-guide/admin/user)。

## 分组升级与回退

如果套餐配置了升级分组，订阅创建时系统会立即尝试把用户切换到该分组，并在订阅中记录购买前分组。订阅到期、被管理员失效或删除时，回退逻辑如下：

1. 订阅中有明确的降级分组时，优先切换到该分组。
2. 没有明确降级分组时，只有用户当前仍处于该订阅记录的升级分组，才会尝试恢复购买前分组。
3. 用户仍有其他有效的升级型订阅，或用户分组已经因其他操作发生变化时，系统可能不会立即回退。

分组变化会刷新用户分组缓存，但不会主动撤销用户的现有登录会话。执行失效或删除前，应先检查用户的当前分组和其他有效订阅。

## 支付方式与续费边界

| 支付方式 | 套餐要求 | 创建订阅的方式 |
| --- | --- | --- |
| 钱包余额 | 开启「允许余额兑换」，用户余额充足。 | 按 `ceil(套餐价格 × QuotaPerUnit)` 扣除内部额度，并立即创建一份订阅。 |
| EPay | 全局 EPay 已启用且有可用支付方式；套餐价格至少为 `0.01`。 | 支付回调成功后创建一份订阅，不需要套餐产品 ID。 |
| Stripe | 套餐填写 Stripe Price ID，全局 Stripe 与 Webhook 配置完整。 | 首次 Checkout 支付确认后创建一份本地订阅。 |
| Creem | 套餐填写 Creem Product ID，全局 Creem 和回调配置完整。 | 支付回调成功后创建一份订阅。 |
| Waffo Pancake | 套餐选择独立的 Waffo Pancake Product ID，全局凭证、店铺和回调配置完整。 | 购买一次性产品并在回调成功后创建一份订阅。 |

<Callout type="warn">
  当前代码不能视为完整的自动续费实现。Stripe Checkout 使用 `subscription` 模式，但本地只在首次 Checkout 支付确认时创建订阅，未处理后续周期扣款续期，也未同步 Stripe 取消或退款事件；EPay、Creem、Waffo Pancake 和钱包余额同样按一次购买创建一份权益。配置 Stripe 前应避免出现第三方持续扣款而本地权益不延长的情况。
</Callout>

用户可以在钱包页面选择「优先订阅」「优先钱包」「仅用订阅」或「仅用钱包」。套餐的「额度用尽后允许使用钱包余额」只决定订阅额度不足时是否允许回退钱包，不会覆盖用户自己的计费偏好。用户侧购买、订阅状态和计费偏好说明参阅[钱包与订阅](/zh/docs/guide/feature-guide/user/wallet)。

## 自动维护机制

订阅到期和周期额度重置由后台维护任务处理：

- 只在 Master 节点运行；
- 服务启动后先执行一次，之后约每分钟检查一次；
- 每批最多处理 300 条记录；
- 到期处理会更新订阅状态，并按条件执行用户分组回退；
- 周期重置只处理仍为 `active` 且尚未到期的订阅。

因此，到期状态和分组回退可能不是在结束时间到达的同一秒完成。若长时间未更新，应检查 Master 节点、后台任务运行情况和应用日志。

## 注意事项

- 套餐价格允许为 `0`，但不同第三方网关可能拒绝零金额或设置更高的最低金额。
- 套餐的币种字段由后端固定保存为 `USD`；界面显示方式和支付平台实际结算币种仍应结合全局配置核对。
- `0` 元套餐若允许余额兑换，可以直接创建订阅；发布前应确认这是否符合运营目的。
- 总额度为 `0` 表示不限额，不表示套餐不可用。
- 限购统计历史上仍存在的订阅记录，而不只统计当前有效订阅。
- 限购会在支付回调创建订阅时再次检查。用户同时打开多个支付会话时，后完成的订单可能已经在第三方扣款，却因达到限购而无法创建本地权益；应避免重复发起支付，并监控支付成功但未发放权益的订单。
- 禁用套餐不会取消既有订阅，也不会自动撤销待支付订单。
- 修改套餐不会批量更新已有权益，但修改重置周期可能影响其后续重置时间。
- 升级和降级分组必须存在；删除或重命名分组前应先检查套餐与用户订阅中的引用。
- Waffo Pancake 的套餐产品与钱包充值产品是不同配置，不要混用产品 ID。
- 对额度、有效期、分组或支付配置进行重大调整前，建议记录变更时间和套餐 ID，便于排查下单与回调跨越变更时产生的差异。

## 常见问题与排查

- **无法新建、编辑或启停套餐**：确认当前账户具有 Admin 或 Root 权限，并由 Root 完成当前版本的支付合规确认。
- **保存时提示套餐标题为空**：标题不能是空字符串或只包含空白字符。
- **价格保存失败**：价格不能为负数，也不能超过 `9999`；第三方支付还可能有独立的最低金额限制。
- **自定义周期不可用**：自定义有效期和自定义额度重置都需要大于 `0` 的秒数。
- **升级或降级分组保存失败**：确认该分组仍存在于当前分组配置中。
- **用户看不到套餐**：检查套餐是否启用、支付合规是否确认，以及用户侧订阅接口是否正常。某个第三方支付按钮是否出现，还取决于产品 ID 和全局网关配置。
- **列表没有显示 EPay 或余额渠道**：支付渠道列只根据三个第三方产品 ID 显示 Stripe、Creem 和 Waffo Pancake，不代表 EPay 或余额兑换一定不可用。
- **支付按钮不可用**：检查套餐产品 ID、全局凭证、回调地址、Webhook 事件、第三方产品状态和金额要求。Stripe Webhook 的可用性还依赖完整的全局 Stripe 配置。
- **禁用套餐后旧订阅仍然有效**：这是预期行为。禁用只控制新的公开展示和购买。
- **修改套餐后旧订阅没有变化**：有效期、总额度、分组和钱包溢出按创建时快照运行；重置周期是会读取当前套餐配置的例外。
- **用户仍提示达到限购数量**：已过期、已取消和管理员添加的记录也计入上限；只有删除对应订阅记录才会释放次数。
- **第三方显示支付成功但没有生成订阅**：检查回调日志、订单状态、套餐是否仍存在，以及回调执行时是否已达到限购。不要直接再次扣款，应先对账并人工确认是否需要补发权益或退款。
- **批量重置显示 `0` 条**：该套餐下没有状态为 `active` 且尚未到期的订阅，或记录已由其他操作处理。
- **订阅到期后用户分组未回退**：检查订阅中的明确降级分组、用户当前分组，以及是否还有其他有效的升级型订阅。
- **Stripe 已产生后续扣款但本地期限未延长**：当前实现没有后续周期续期和取消/退款同步。保留订单与第三方事件信息，停止重复处理，并由维护人员核对用户权益和第三方订阅状态。

## 相关文档

- [用户管理](/zh/docs/guide/feature-guide/admin/user)：为指定用户添加、重置、失效或删除订阅。
- [钱包与订阅](/zh/docs/guide/feature-guide/user/wallet)：用户购买套餐、查看订阅和选择计费偏好。
- [计费与支付配置](/zh/docs/guide/feature-guide/system-settings/settings-billing)：配置额度单位、货币显示、分组、支付合规和支付网关。
